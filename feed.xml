<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="3.10.0">Jekyll</generator><link href="/feed.xml" rel="self" type="application/atom+xml" /><link href="/" rel="alternate" type="text/html" /><updated>2026-08-03T22:53:43+00:00</updated><id>/feed.xml</id><title type="html">Minh Nhật’s blog</title><entry><title type="html">103. Adding a Column to a High-Traffic PostgreSQL Table: The Hidden Cost of ALTER TABLE</title><link href="/technical/103-adding-a-column-to-high-traffic-table" rel="alternate" type="text/html" title="103. Adding a Column to a High-Traffic PostgreSQL Table: The Hidden Cost of ALTER TABLE" /><published>2026-08-02T08:02:00+00:00</published><updated>2026-08-02T08:02:00+00:00</updated><id>/technical/103-adding-a-column-to-high-traffic-table</id><content type="html" xml:base="/technical/103-adding-a-column-to-high-traffic-table"><![CDATA[<p><strong>AI Assisted</strong></p>

<h2 id="introduction">Introduction</h2>

<p>Adding a new column is often considered a routine schema change. In PostgreSQL, the SQL statement itself is straightforward, and modern versions can perform many column additions as metadata-only operations. Because of this, developers frequently assume that adding a nullable column is essentially free.</p>

<p>The hidden cost, however, is not always the data modification. It is the lock required before PostgreSQL can modify the table definition. On a heavily trafficked table containing hundreds of millions of rows, acquiring that lock can become the most dangerous part of the migration.</p>

<h2 id="the-first-thing-postgresql-does-is-acquire-an-exclusive-lock">The First Thing PostgreSQL Does Is Acquire an Exclusive Lock</h2>

<p>Before PostgreSQL changes a table’s schema, it acquires an <strong>ACCESS EXCLUSIVE</strong> lock on the table. This is the strongest table-level lock in PostgreSQL and conflicts with every other lock mode.</p>

<p>As a result, while the lock is held:</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">SELECT</code> queries are blocked.</li>
  <li><code class="language-plaintext highlighter-rouge">INSERT</code>, <code class="language-plaintext highlighter-rouge">UPDATE</code>, and <code class="language-plaintext highlighter-rouge">DELETE</code> statements are blocked.</li>
  <li>Other schema changes are blocked.</li>
  <li>Any transaction attempting to access the table must wait.</li>
</ul>

<p>Even if the actual schema modification takes only a few milliseconds, PostgreSQL cannot begin the operation until it successfully acquires this lock.</p>

<h2 id="the-real-problem-is-waiting-for-the-lock">The Real Problem Is Waiting for the Lock</h2>

<p>In production systems, there is almost always at least one transaction already using the table. It may be a business transaction, a reporting query, a batch job, or an application request that unexpectedly runs for several seconds.</p>

<p>Suppose a transaction has been reading from the <code class="language-plaintext highlighter-rouge">orders</code> table for thirty seconds. When an <code class="language-plaintext highlighter-rouge">ALTER TABLE</code> statement is issued, PostgreSQL cannot immediately acquire the required <code class="language-plaintext highlighter-rouge">ACCESS EXCLUSIVE</code> lock and must wait for the existing transaction to finish.</p>

<p>This is where the migration becomes dangerous. While the <code class="language-plaintext highlighter-rouge">ALTER TABLE</code> statement is waiting, every new query that requires the table begins to queue behind it. PostgreSQL’s lock manager preserves lock ordering, meaning subsequent requests cannot bypass the pending exclusive lock request. What began as a single long-running transaction can quickly develop into a lock queue affecting hundreds or even thousands of application requests.</p>

<h2 id="how-a-single-long-transaction-can-cascade-into-an-outage">How a Single Long Transaction Can Cascade into an Outage</h2>

<p>The database itself is usually not frozen, but one busy table effectively becomes unavailable. Application requests continue arriving, yet each request that needs the locked table waits for the exclusive lock to be resolved.</p>

<p>As waiting requests accumulate, database connections remain occupied for longer periods. Eventually, the application’s connection pool becomes exhausted because every connection is blocked waiting for the same table. Once no connections remain available, new requests begin to time out, error rates increase, and the application may appear to be completely unavailable despite PostgreSQL continuing to run normally.</p>

<p>This cascading failure is often far more damaging than the schema change itself.</p>

<h2 id="why-large-tables-increase-the-risk">Why Large Tables Increase the Risk</h2>

<p>A table containing 300 million rows is not necessarily slower to add a nullable column in modern PostgreSQL. However, it is typically one of the busiest tables in the system. High request volume increases the probability that long-running transactions already exist when the migration begins, making it more difficult to obtain the required exclusive lock without disrupting production traffic.</p>

<p>For this reason, migration risk is often driven more by concurrency than by table size alone.</p>

<h2 id="a-safer-migration-strategy">A Safer Migration Strategy</h2>

<p>Before executing any schema change on a high-traffic table, first identify long-running transactions using PostgreSQL monitoring views such as <code class="language-plaintext highlighter-rouge">pg_stat_activity</code>. Schedule migrations during periods of lower traffic whenever possible, and configure an appropriate <code class="language-plaintext highlighter-rouge">lock_timeout</code> so that the migration fails quickly rather than waiting indefinitely for an exclusive lock.</p>

<p>Second, <code class="language-plaintext highlighter-rouge">add the new column as nullable</code> without a default value whenever possible. This minimizes the amount of work required by the database engine and reduces the likelihood of long-running locks.</p>

<p>Next, <code class="language-plaintext highlighter-rouge">deploy an application version that understands both the old and new schema</code>. New records should populate the new column, while existing records continue to function correctly even if the column is still NULL. This approach maintains backward compatibility and allows deployment to be rolled back independently of the database migration.</p>

<p>After the application has been writing the new column successfully, perform a gradual backfill. <code class="language-plaintext highlighter-rouge">Rather than updating all 300 million records in a single transaction, process the data in small batches</code>. Between batches, monitor database CPU utilization, disk I/O, replication lag, lock contention, and application latency. The objective is to spread the workload over time without degrading production performance.</p>

<p>Finally, once every existing row has been populated and the application no longer depends on NULL values, enforce additional constraints such as NOT NULL if required. By separating structural changes from data migration, each deployment step remains smaller, safer, and easier to recover from.</p>

<h2 id="lessons-learned">Lessons Learned</h2>

<p>When engineers think about adding a column, they often focus on how long PostgreSQL needs to modify the table. In practice, the greater risk is usually how long PostgreSQL waits to acquire the required <code class="language-plaintext highlighter-rouge">ACCESS EXCLUSIVE</code> lock. A migration that executes in milliseconds can still trigger application-wide failures if it becomes blocked behind a long-running transaction on a heavily used table.</p>

<p>Understanding PostgreSQL’s locking behavior is therefore essential for designing safe, zero-downtime schema migrations. In production systems, managing concurrency is frequently more important than the SQL statement itself.</p>]]></content><author><name></name></author><category term="technical" /><summary type="html"><![CDATA[AI Assisted]]></summary></entry><entry><title type="html">102. 4 cấp độ nhận thức: Bạn Đang Ở Đâu Trong Bức Tranh Tri Thức?</title><link href="/opinion/102-4-cap-do-nhan-thuc" rel="alternate" type="text/html" title="102. 4 cấp độ nhận thức: Bạn Đang Ở Đâu Trong Bức Tranh Tri Thức?" /><published>2026-07-30T08:02:00+00:00</published><updated>2026-07-30T08:02:00+00:00</updated><id>/opinion/102-4-cap-do-nhan-thuc</id><content type="html" xml:base="/opinion/102-4-cap-do-nhan-thuc"><![CDATA[<p><strong>Phân tích dựa trên AI</strong></p>

<p>Có bao giờ bạn rơi vào tình huống: Bạn nghĩ mình đã chuẩn bị kỹ cho một kế hoạch, nhưng cuối cùng lại thất bại bởi một rắc rối mà bạn <em>chưa từng nghĩ nó tồn tại</em>?</p>

<p>Đó là lúc bạn chạm trán với giới hạn nhận thức của chính mình. Khung tư duy <strong>“4 Cấp Độ Nhận Thức”</strong> (nổi tiếng qua tuyên bố của cựu Bộ trưởng Quốc phòng Mỹ Donald Rumsfeld) là công cụ tuyệt vời giúp bạn định vị trí tuệ, sự hiểu biết và cả những điểm mù của bản thân.</p>

<h3 id="1-known-knowns-biết-điều-mình-biết">1. Known Knowns (Biết điều mình biết)</h3>

<p>Đây là vùng an toàn — những tri thức và kỹ năng bạn đã làm chủ hoàn toàn.</p>

<ul>
  <li><strong>Ví dụ:</strong> Bạn biết lái xe máy, biết 1 + 1 = 2, hay biết cách gửi một email công việc.</li>
  <li><strong>Đặc điểm:</strong> Mang lại sự tự tin và giúp bạn xử lý công việc hằng ngày một cách mượt mà.</li>
</ul>

<h3 id="2-known-unknowns-biết-điều-mình-không-biết">2. Known Unknowns (Biết điều mình KHÔNG biết)</h3>

<p>Danh sách những thứ bạn nhận thức được sự tồn tại của chúng, nhưng bản thân chưa có kiến thức hay kỹ năng về nó.</p>

<ul>
  <li><strong>Ví dụ:</strong> Bạn biết tiếng Trung có tồn tại nhưng bạn không biết nói; hoặc biết lập trình Python rất hữu ích nhưng bạn chưa học.</li>
  <li><strong>Đặc điểm:</strong> Đây là <strong>động lực học hỏi</strong>. Khi biết mình thiếu gì, bạn mới có thể chủ động tìm sách đọc, đăng ký khóa học hay nhờ người chỉ dạy.</li>
</ul>

<h3 id="3-unknown-knowns-không-biết-điều-mình-biết">3. Unknown Knowns (KHÔNG biết điều mình biết)</h3>

<p>Những tri thức, bản năng hoặc tiềm năng bạn sở hữu nhưng vô tình quên mất, đè nén xuống, hoặc chưa từng nhận ra cho đến khi hoàn cảnh bắt buộc.</p>

<ul>
  <li><strong>Ví dụ:</strong> Bạn nghĩ mình sợ nói trước đám đông. Nhưng khi công ty gặp sự cố đột xuất và bắt bạn đứng lên giải trình, bạn lại nói rất logic và thuyết phục.</li>
  <li><strong>Đặc điểm:</strong> Vùng này chứa đựng năng lực ngầm và sự sáng tạo bị ẩn giấu. Trải nghiệm mới sẽ giúp bạn “khai quật” nó.</li>
</ul>

<h3 id="4-unknown-unknowns-không-biết-điều-mình-không-biết">4. Unknown Unknowns (KHÔNG biết điều mình KHÔNG biết)</h3>

<p>Đây là những <strong>điểm mù tuyệt đối</strong> — các rủi ro hoặc tri thức nằm ngoài hoàn toàn phạm vi hình dung của bạn.</p>

<ul>
  <li><strong>Ví dụ:</strong> Bạn mở quán cà phê và chuẩn bị kỹ về vốn, mặt bằng. Đột nhiên một đại dịch toàn cầu bùng nổ khiến mọi dịch vụ đóng cửa. Trước đó, bạn chưa từng nghĩ kịch bản này có thể xảy ra.</li>
  <li><strong>Đặc điểm:</strong> Vùng nguy hiểm nhất vì nó dễ gây ra sự tự tin thái quá, nhưng cũng là nơi chứa đựng những cơ hội bứt phá lớn nhất.</li>
</ul>

<hr />

<h3 id="ai-giúp-bạn-mở-rộng-biên-giới-nhận-thức-như-thế-nào">AI Giúp Bạn Mở Rộng Biên Giới Nhận Thức Như Thế Nào?</h3>

<p>Trong kỷ nguyên công nghệ, Trí tuệ nhân tạo (AI) chính là công cụ đắc lực giúp bạn làm chủ 4 cấp độ này:</p>

<ul>
  <li><strong>Học nhanh hơn (<em>Known Unknowns</em> $\rightarrow$ <em>Known Knowns</em>):</strong> Khi biết mình thiếu kiến thức gì, AI đóng vai trò gia sư 24/7 giúp bạn cô đọng thông tin và giải thích những khái niệm phức tạp một cách dễ hiểu nhất.</li>
  <li><strong>Bật mở tiềm năng (<em>Unknown Knowns</em>):</strong> AI làm người đối thoại (sounding board), giúp bạn hệ thống hóa những ý tưởng thô và kinh nghiệm rải rác thành bài viết hay kế hoạch hoàn chỉnh.</li>
  <li><strong>Soi rọi điểm mù (<em>Unknown Unknowns</em>):</strong> Phản biện kế hoạch bằng AI giúp bạn phát hiện những rủi ro về thị trường, pháp lý hay tâm lý mà góc nhìn cá nhân chưa từng tính đến.</li>
</ul>

<hr />

<h3 id="lời-kết">Lời Kết</h3>

<p>Người thông thái không phải là người biết tất cả mọi thứ, mà là người biết rõ giới hạn hiểu biết của mình nằm ở đâu. Hiểu được 4 cấp độ nhận thức giúp bạn giữ sự khiêm tốn, chủ động đặt câu hỏi và luôn sẵn sàng học hỏi.</p>

<p>Khi kết hợp tư duy này với sức mạnh của AI, bạn không chỉ học nhanh hơn mà còn sở hữu một “con mắt thứ ba” để nhìn thấu những điểm mù — nơi cất giấu cả rủi ro lớn nhất lẫn cơ hội bứt phá nhất.</p>]]></content><author><name></name></author><category term="opinion" /><summary type="html"><![CDATA[Phân tích dựa trên AI]]></summary></entry><entry><title type="html">101. Phân tích sách Khôn Ngoan chương 12.</title><link href="/philosophy/101-phan-tich-sach-khon-ngoan-chuong-12" rel="alternate" type="text/html" title="101. Phân tích sách Khôn Ngoan chương 12." /><published>2026-07-19T08:02:00+00:00</published><updated>2026-07-19T08:02:00+00:00</updated><id>/philosophy/101-phan-tich-sach-khon-ngoan-chuong-12</id><content type="html" xml:base="/philosophy/101-phan-tich-sach-khon-ngoan-chuong-12"><![CDATA[<p><strong>Phân tích dựa trên AI</strong></p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Sách Khôn Ngoan chương 12

13
Lạy Thiên Chúa,
ngoài Ngài ra, chẳng còn thần nào khác
để Ngài phải chứng tỏ rằng
các phán quyết của Ngài không bất công.

16
Chính do sức mạnh của Chúa
mà Chúa hành động công minh,
và vì Chúa làm bá chủ vạn vật,
nên Chúa nương tay với muôn loài.

17

Khi không có ai tin rằng Chúa nắm trọn quyền năng,
thì Ngài tỏ sức mạnh ;
còn ai đã biết mà vẫn to gan, thì Ngài trị tội.

18
Nhưng Chúa xử khoan hồng vì Ngài làm chủ được sức mạnh.
Ngài lấy lượng từ bi cao cả mà cai quản chúng con,
nhưng có thể sử dụng quyền năng bất cứ khi nào Ngài muốn.

19
Làm như thế Chúa đã dạy dân rằng :
người công chính phải có lòng nhân ái.
Ngài đã cho con cái niềm hy vọng tràn trề
là người có tội được Ngài ban ơn sám hối. 

</code></pre></div></div>
<p>Đoạn Kinh Thánh trên được trích từ <strong>Sách Khôn Ngoan (Kn 12,13.16-19)</strong>. Đây là một trong những đoạn văn tuyệt đẹp và sâu sắc nhất Cựu Ước phác họa chân dung của Thiên Chúa: Ngài không chỉ là Đấng Toàn Năng có chủ quyền tuyệt đối, mà trên hết, Ngài là Người Cha đầy lòng thương xót và chính trực.</p>

<p>Dưới đây là phân tích chi tiết đoạn văn bản theo các chủ đề cốt lõi:</p>

<hr />

<h3 id="1-chủ-quyền-độc-tôn-và-công-lý-tuyệt-đối-câu-13-16">1. Chủ quyền độc tôn và Công lý tuyệt đối (Câu 13, 16)</h3>

<blockquote>
  <p><em>“Lạy Thiên Chúa, ngoài Ngài ra, chẳng còn thần nào khác… Chính do sức mạnh của Chúa mà Chúa hành động công minh”</em></p>
</blockquote>

<ul>
  <li><strong>Vị thế độc tôn:</strong> Tác giả khẳng định niềm tin độc thần tuyệt đối. Thiên Chúa không cần phải chứng minh hay biện hộ cho các phán quyết của mình với bất kỳ ai, bởi vì không có một thế lực nào đứng ngang hàng hay cao hơn Ngài.</li>
  <li><strong>Mối liên kết giữa Sức mạnh và Công lý:</strong> Ở thế gian, kẻ mạnh thường dùng quyền lực để áp bức và hành động bất công. Nhưng với Thiên Chúa thì ngược lại: <strong>chính vì Ngài là Đấng Toàn Năng nên Ngài luôn công minh</strong>. Ngài không cần phải thiên vị, không sợ hãi áp lực, và không cần dùng đến sự bất công để duy trì vương quyền của mình.</li>
</ul>

<h3 id="2-nghịch-lý-đầy-yêu-thương-quyền-năng-đi-đôi-với-khoan-dung-câu-16-17-18">2. Nghịch lý đầy yêu thương: Quyền năng đi đôi với Khoan dung (Câu 16, 17, 18)</h3>

<blockquote>
  <p><em>“…vì Chúa làm bá chủ vạn vật, nên Chúa nương tay với muôn loài.”</em>
<em>“Nhưng Chúa xử khoan hồng vì Ngài làm chủ được sức mạnh.”</em></p>
</blockquote>

<p>Đây là đỉnh cao thần học của đoạn kinh này. Sự toàn năng của Thiên Chúa không được thể hiện qua sự bạo tàn hay hủy diệt, mà qua <strong>sự tự kiềm chế và lòng nhẫn nại</strong>.</p>

<ul>
  <li><strong>Lòng nương tay với muôn loài:</strong> Vì Ngài là Đấng tạo dựng và làm chủ vạn vật, Ngài nhìn tạo vật bằng ánh mắt của một người chủ đầy trách nhiệm và yêu thương, chứ không phải kẻ độc tài.</li>
  <li><strong>Trừng phạt kẻ kiêu ngạo:</strong> Ngài chỉ bày tỏ sức mạnh trừng phạt đối với những kẻ cố tình thách thức quyền năng của Ngài (không tin) hoặc những kẻ biết luật mà vẫn cố tình phạm tội (to gan). Đó là để duy trì trật tự và công lý.</li>
  <li><strong>Làm chủ được sức mạnh:</strong> Con người khi nóng giận thường mất kiểm soát và dùng bạo lực. Thiên Chúa thì khác, Ngài hoàn toàn “làm chủ được sức mạnh” của mình, luôn giữ cho quyền năng ấy được bao bọc bởi “lượng từ bi cao cả”.</li>
</ul>

<h3 id="3-bài-học-cho-con-người-và-niềm-hy-vọng-sám-hối-câu-19">3. Bài học cho con người và Niềm hy vọng Sám hối (Câu 19)</h3>

<blockquote>
  <p><em>“Làm như thế Chúa đã dạy dân rằng: người công chính phải có lòng nhân ái. Ngài đã cho con cái niềm hy vọng tràn trề là người có tội được Ngài ban ơn sám hối.”</em></p>
</blockquote>

<p>Cách hành xử của Thiên Chúa trở thành khuôn mẫu đạo đức và đem lại tin mừng cho nhân loại:</p>

<ul>
  <li><strong>Bài học về lòng nhân ái:</strong> Người “công chính” theo tiêu chuẩn của Thiên Chúa không phải là người khắt khe, giáo điều hay thích phán xét, mà phải là người <strong>“có lòng nhân ái”</strong>. Nếu Thiên Chúa là Đấng Công Chính đã tha thứ và nhẫn nại với ta, thì ta cũng phải đối xử với tha nhân như vậy.</li>
  <li><strong>Niềm hy vọng tràn trề:</strong> Đoạn kết mở ra một chân trời đầy ánh sáng. Thiên Chúa không vội vã tiêu diệt tội nhân, Ngài chậm giận và dành cho họ thời gian. Sự trì hoãn hình phạt của Chúa không phải là bất lực, mà là một <strong>“ân huệ”</strong> để con người có cơ hội nhận ra sai lầm và quay bước trở về (sám hối).</li>
</ul>

<hr />

<h3 id="ý-nghĩa-đúc-kết">Ý nghĩa đúc kết</h3>

<p>Đoạn Kinh Thánh này là một lời an ủi lớn lao cho người tín hữu. Nó đảo lộn cái nhìn thông thường của con người về quyền lực. Quyền lực đích thực của Thiên Chúa được thể hiện rực rỡ nhất không phải trong những sấm sét trừng phạt, mà là trong <strong>lòng thương xót và ơn tha thứ</strong>. Ngài mạnh mẽ để bảo vệ công lý, nhưng Ngài cũng đủ bao dung để chờ đợi tội nhân hoán cải.</p>]]></content><author><name></name></author><category term="philosophy" /><summary type="html"><![CDATA[Phân tích dựa trên AI]]></summary></entry><entry><title type="html">100. The race condition in Java</title><link href="/technical/100-race-conditions-in-java" rel="alternate" type="text/html" title="100. The race condition in Java" /><published>2026-04-06T08:02:00+00:00</published><updated>2026-04-06T08:02:00+00:00</updated><id>/technical/100-race-conditions-in-java</id><content type="html" xml:base="/technical/100-race-conditions-in-java"><![CDATA[<p>Note: This blogs are generated by Claude.</p>

<h1 id="race-conditions-in-java-what-every-developer-should-know">Race Conditions in Java: What Every Developer Should Know</h1>

<blockquote>
  <p><em>“Concurrency bugs are the hardest bugs to find, reproduce, and fix. Understanding race conditions is the first step to writing code that doesn’t lie to you in production.”</em></p>
</blockquote>

<hr />

<h2 id="the-core-assumption">The Core Assumption</h2>

<blockquote>
  <p><em>Any thread can be paused at any point — between any two instructions — and another thread can run.</em></p>
</blockquote>

<p>Not between methods. Not between lines. Between any two bytecode/machine instructions. The OS scheduler is ruthless and gives you zero guarantees about timing.</p>

<p>Take this innocent-looking line:</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">lastNumber</span><span class="o">.</span><span class="na">set</span><span class="o">(</span><span class="n">i</span><span class="o">);</span>
<span class="n">lastFactors</span><span class="o">.</span><span class="na">set</span><span class="o">(</span><span class="n">factors</span><span class="o">);</span>
</code></pre></div></div>

<p>You see 2 lines. The scheduler sees 2 <strong>independent pause opportunities</strong>:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>──── Thread A ────────────────────────────────────────
lastNumber.set(7);
        ↑ scheduler pauses Thread A RIGHT HERE
                    ──── Thread B reads ────────────────
                    lastNumber.get()  → 7      ✓
                    lastFactors.get() → [2,3]  ✗ (still old!)
        ↓ Thread A resumes
lastFactors.set([7]);   ← too late, damage done
</code></pre></div></div>

<hr />

<h2 id="what-is-a-race-condition">What Is a Race Condition?</h2>

<p>A <strong>race condition</strong> occurs when the correctness of a program depends on the relative timing or interleaving of operations across multiple threads. When two or more threads access shared mutable state simultaneously — and at least one of them is writing — the result becomes unpredictable.</p>

<p>The dangerous part? Your code may look perfectly correct. It may pass every unit test. It may run fine in development. Then, under production load with multiple threads racing through the same code path, it silently produces wrong results.</p>

<p>The term “race” is apt: threads are literally racing to execute operations, and whoever wins the race determines the outcome — not the logic you intended.</p>

<hr />

<h2 id="why-is-it-so-hard-to-catch">Why Is It So Hard to Catch?</h2>

<p>Race conditions are <strong>non-deterministic</strong>. The bug only appears when threads interleave in a specific way. That interleaving depends on:</p>

<ul>
  <li>CPU scheduling decisions</li>
  <li>Garbage collection pauses</li>
  <li>System load at that moment</li>
  <li>Hardware architecture (number of cores, cache behaviour)</li>
</ul>

<p>A test suite running sequentially will never expose it. A bug might appear once every million requests — and vanish the moment you attach a debugger, because the debugger changes thread timing.</p>

<p>This is what makes race conditions genuinely dangerous: they hide until the worst possible moment.</p>

<hr />

<h2 id="common-types-of-race-conditions">Common Types of Race Conditions</h2>

<h3 id="1-read-modify-write">1. Read-Modify-Write</h3>

<p>You read a value, compute something from it, then write the modified value back. If another thread reads the same value before you write, one update is silently lost.</p>

<p><strong>The classic example — a shared counter:</strong></p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// Shared state</span>
<span class="kd">private</span> <span class="kt">int</span> <span class="n">counter</span> <span class="o">=</span> <span class="mi">0</span><span class="o">;</span>

<span class="c1">// Thread 1 and Thread 2 both call this simultaneously</span>
<span class="kd">public</span> <span class="kt">void</span> <span class="nf">increment</span><span class="o">()</span> <span class="o">{</span>
    <span class="n">counter</span><span class="o">++;</span> <span class="c1">// NOT atomic — this is read, add, write: 3 operations</span>
<span class="o">}</span>
</code></pre></div></div>

<p><strong>What actually happens:</strong></p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Thread 1: reads counter = 5
Thread 2: reads counter = 5       ← both see 5 before either writes
Thread 1: writes counter = 6
Thread 2: writes counter = 6      ← Thread 1's increment is lost
</code></pre></div></div>

<p>Expected: <code class="language-plaintext highlighter-rouge">counter = 7</code>. Actual: <code class="language-plaintext highlighter-rouge">counter = 6</code>. This is called a <strong>lost update</strong>.</p>

<p><strong>Fix — use atomic operations:</strong></p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">private</span> <span class="kd">final</span> <span class="nc">AtomicInteger</span> <span class="n">counter</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">AtomicInteger</span><span class="o">(</span><span class="mi">0</span><span class="o">);</span>

<span class="kd">public</span> <span class="kt">void</span> <span class="nf">increment</span><span class="o">()</span> <span class="o">{</span>
    <span class="n">counter</span><span class="o">.</span><span class="na">incrementAndGet</span><span class="o">();</span> <span class="c1">// single atomic CAS operation</span>
<span class="o">}</span>
</code></pre></div></div>

<p>Or protect the compound action with a lock:</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">private</span> <span class="kt">int</span> <span class="n">counter</span> <span class="o">=</span> <span class="mi">0</span><span class="o">;</span>
<span class="kd">private</span> <span class="kd">final</span> <span class="nc">Object</span> <span class="n">lock</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">Object</span><span class="o">();</span>

<span class="kd">public</span> <span class="kt">void</span> <span class="nf">increment</span><span class="o">()</span> <span class="o">{</span>
    <span class="kd">synchronized</span> <span class="o">(</span><span class="n">lock</span><span class="o">)</span> <span class="o">{</span>
        <span class="n">counter</span><span class="o">++;</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<hr />

<h3 id="2-check-then-act">2. Check-Then-Act</h3>

<p>You check a condition and then act on the assumption that the condition is still true. But another thread can invalidate the condition between your check and your action.</p>

<p><strong>Example — conditional map insertion:</strong></p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// NOT thread-safe</span>
<span class="k">if</span> <span class="o">(!</span><span class="n">map</span><span class="o">.</span><span class="na">containsKey</span><span class="o">(</span><span class="n">key</span><span class="o">))</span> <span class="o">{</span>
    <span class="n">map</span><span class="o">.</span><span class="na">put</span><span class="o">(</span><span class="n">key</span><span class="o">,</span> <span class="n">value</span><span class="o">);</span> <span class="c1">// another thread may have inserted between these two lines</span>
<span class="o">}</span>
</code></pre></div></div>

<p>Both threads can pass the <code class="language-plaintext highlighter-rouge">containsKey</code> check simultaneously and both call <code class="language-plaintext highlighter-rouge">put</code>, defeating any uniqueness guarantee.</p>

<p><strong>Another classic — lazy initialisation:</strong></p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// NOT thread-safe</span>
<span class="k">if</span> <span class="o">(</span><span class="n">instance</span> <span class="o">==</span> <span class="kc">null</span><span class="o">)</span> <span class="o">{</span>
    <span class="n">instance</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">ExpensiveObject</span><span class="o">();</span> <span class="c1">// two threads can both pass the null check</span>
<span class="o">}</span>
</code></pre></div></div>

<p><strong>Fix — use atomic compound operations:</strong></p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// ConcurrentHashMap provides atomic compound operations</span>
<span class="n">map</span><span class="o">.</span><span class="na">putIfAbsent</span><span class="o">(</span><span class="n">key</span><span class="o">,</span> <span class="n">value</span><span class="o">);</span>

<span class="c1">// Or for lazy init — use double-checked locking correctly</span>
<span class="kd">private</span> <span class="kd">volatile</span> <span class="nc">ExpensiveObject</span> <span class="n">instance</span><span class="o">;</span>

<span class="kd">public</span> <span class="nc">ExpensiveObject</span> <span class="nf">getInstance</span><span class="o">()</span> <span class="o">{</span>
    <span class="k">if</span> <span class="o">(</span><span class="n">instance</span> <span class="o">==</span> <span class="kc">null</span><span class="o">)</span> <span class="o">{</span>
        <span class="kd">synchronized</span> <span class="o">(</span><span class="k">this</span><span class="o">)</span> <span class="o">{</span>
            <span class="k">if</span> <span class="o">(</span><span class="n">instance</span> <span class="o">==</span> <span class="kc">null</span><span class="o">)</span> <span class="o">{</span>           <span class="c1">// second check inside the lock</span>
                <span class="n">instance</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">ExpensiveObject</span><span class="o">();</span>
            <span class="o">}</span>
        <span class="o">}</span>
    <span class="o">}</span>
    <span class="k">return</span> <span class="n">instance</span><span class="o">;</span>
<span class="o">}</span>
</code></pre></div></div>

<p>Note: <code class="language-plaintext highlighter-rouge">volatile</code> is essential here to prevent the compiler from reordering the write to <code class="language-plaintext highlighter-rouge">instance</code> before the constructor finishes.</p>

<hr />

<h3 id="3-stale-data--visibility-race">3. Stale Data / Visibility Race</h3>

<p>This one doesn’t even need a compound action. A single read can return an outdated value because of CPU caching and compiler/JVM reordering. One thread writes a value, but another thread reads from its local cache and never sees the update.</p>

<p><strong>Example — a stop flag:</strong></p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// NOT thread-safe</span>
<span class="kd">private</span> <span class="kt">boolean</span> <span class="n">running</span> <span class="o">=</span> <span class="kc">true</span><span class="o">;</span>

<span class="c1">// Thread 2 calls this</span>
<span class="kd">public</span> <span class="kt">void</span> <span class="nf">stop</span><span class="o">()</span> <span class="o">{</span>
    <span class="n">running</span> <span class="o">=</span> <span class="kc">false</span><span class="o">;</span>
<span class="o">}</span>

<span class="c1">// Thread 1 runs this — may loop forever</span>
<span class="kd">public</span> <span class="kt">void</span> <span class="nf">run</span><span class="o">()</span> <span class="o">{</span>
    <span class="k">while</span> <span class="o">(</span><span class="n">running</span><span class="o">)</span> <span class="o">{</span>
        <span class="n">doWork</span><span class="o">();</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p>Thread 1 may cache <code class="language-plaintext highlighter-rouge">running = true</code> in a CPU register and never check main memory again. Thread 2’s write is invisible to it.</p>

<p><strong>Why Thread 1 never sees the change</strong></p>

<p><strong>1. The compiler optimizes</strong>
The compiler sees that Thread 1 never modifies <code class="language-plaintext highlighter-rouge">running</code> itself, so it assumes the value can’t change. It rewrites the loop roughly as:</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// Compiler transforms this:</span>
<span class="k">while</span> <span class="o">(</span><span class="n">running</span><span class="o">)</span> <span class="o">{</span> <span class="n">doWork</span><span class="o">();</span> <span class="o">}</span>

<span class="c1">// Into something like this:</span>
<span class="kt">boolean</span> <span class="n">cached</span> <span class="o">=</span> <span class="n">running</span><span class="o">;</span> <span class="c1">// read once</span>
<span class="k">while</span> <span class="o">(</span><span class="n">cached</span><span class="o">)</span> <span class="o">{</span> <span class="n">doWork</span><span class="o">();</span> <span class="o">}</span> <span class="c1">// never re-reads memory</span>
</code></pre></div></div>

<p><strong>2. The CPU has its own cache (L1/L2)</strong>
Each CPU core has its own cache layer. When Thread 1 reads <code class="language-plaintext highlighter-rouge">running</code>, it loads the value into its core’s cache (or a register). Thread 2 runs on a <em>different</em> core, writes <code class="language-plaintext highlighter-rouge">false</code> to <em>its</em> cache — but that write may not propagate to Thread 1’s cache immediately.</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Core 1 cache: running = true  ← Thread 1 reads this, stuck here
Core 2 cache: running = false ← Thread 2 wrote this
         RAM: running = ???   ← may not even be updated yet
</code></pre></div></div>

<p><strong>3. CPU instruction reordering</strong>
Modern CPUs execute instructions out of order for performance. Even if Thread 2 writes <code class="language-plaintext highlighter-rouge">false</code>, there’s no guarantee Thread 1 will observe that write in any particular timeframe without a <strong>memory barrier</strong>.</p>

<p><strong>Fix — use <code class="language-plaintext highlighter-rouge">volatile</code>:</strong></p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">private</span> <span class="kd">volatile</span> <span class="kt">boolean</span> <span class="n">running</span> <span class="o">=</span> <span class="kc">true</span><span class="o">;</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">volatile</code> guarantees that writes are immediately flushed to main memory and reads always fetch from main memory, establishing <strong>happens-before</strong> between the write in Thread 2 and the read in Thread 1.</p>

<hr />

<h3 id="4-deadlock">4. Deadlock</h3>

<p>A liveness problem rather than a data corruption issue, but closely related to concurrency. Two threads each hold a lock the other needs, so both wait forever — the application hangs.</p>

<p><strong>Example:</strong></p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">Object</span> <span class="n">lockA</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">Object</span><span class="o">();</span>
<span class="nc">Object</span> <span class="n">lockB</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">Object</span><span class="o">();</span>

<span class="c1">// Thread 1</span>
<span class="kd">synchronized</span> <span class="o">(</span><span class="n">lockA</span><span class="o">)</span> <span class="o">{</span>
    <span class="kd">synchronized</span> <span class="o">(</span><span class="n">lockB</span><span class="o">)</span> <span class="o">{</span> <span class="c1">// waits for Thread 2 to release B</span>
        <span class="c1">// ...</span>
    <span class="o">}</span>
<span class="o">}</span>

<span class="c1">// Thread 2</span>
<span class="kd">synchronized</span> <span class="o">(</span><span class="n">lockB</span><span class="o">)</span> <span class="o">{</span>
    <span class="kd">synchronized</span> <span class="o">(</span><span class="n">lockA</span><span class="o">)</span> <span class="o">{</span> <span class="c1">// waits for Thread 1 to release A → DEADLOCK</span>
        <span class="c1">// ...</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p><strong>Fix — always acquire locks in a consistent global order:</strong></p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// Both threads acquire lockA before lockB — deadlock impossible</span>
<span class="kd">synchronized</span> <span class="o">(</span><span class="n">lockA</span><span class="o">)</span> <span class="o">{</span>
    <span class="kd">synchronized</span> <span class="o">(</span><span class="n">lockB</span><span class="o">)</span> <span class="o">{</span>
        <span class="c1">// ...</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<p>Or use <code class="language-plaintext highlighter-rouge">tryLock</code> with a timeout from <code class="language-plaintext highlighter-rouge">java.util.concurrent.locks</code>:</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">ReentrantLock</span> <span class="n">lockA</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">ReentrantLock</span><span class="o">();</span>
<span class="nc">ReentrantLock</span> <span class="n">lockB</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">ReentrantLock</span><span class="o">();</span>

<span class="k">if</span> <span class="o">(</span><span class="n">lockA</span><span class="o">.</span><span class="na">tryLock</span><span class="o">(</span><span class="mi">1</span><span class="o">,</span> <span class="nc">TimeUnit</span><span class="o">.</span><span class="na">SECONDS</span><span class="o">))</span> <span class="o">{</span>
    <span class="k">try</span> <span class="o">{</span>
        <span class="k">if</span> <span class="o">(</span><span class="n">lockB</span><span class="o">.</span><span class="na">tryLock</span><span class="o">(</span><span class="mi">1</span><span class="o">,</span> <span class="nc">TimeUnit</span><span class="o">.</span><span class="na">SECONDS</span><span class="o">))</span> <span class="o">{</span>
            <span class="k">try</span> <span class="o">{</span>
                <span class="c1">// do work</span>
            <span class="o">}</span> <span class="k">finally</span> <span class="o">{</span>
                <span class="n">lockB</span><span class="o">.</span><span class="na">unlock</span><span class="o">();</span>
            <span class="o">}</span>
        <span class="o">}</span>
    <span class="o">}</span> <span class="k">finally</span> <span class="o">{</span>
        <span class="n">lockA</span><span class="o">.</span><span class="na">unlock</span><span class="o">();</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div>

<hr />

<h3 id="5-livelock">5. Livelock</h3>

<p>Similar to deadlock, but threads aren’t blocked — they keep actively responding to each other and making no progress. Like two people in a corridor repeatedly stepping in the same direction to let the other pass.</p>

<p><strong>Conceptual example:</strong></p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// Thread 1: keeps retrying if Thread 2 is also retrying</span>
<span class="k">while</span> <span class="o">(</span><span class="n">thread2IsRetrying</span><span class="o">)</span> <span class="o">{</span>
    <span class="nc">Thread</span><span class="o">.</span><span class="na">sleep</span><span class="o">(</span><span class="mi">100</span><span class="o">);</span>
    <span class="n">retry</span><span class="o">();</span>
<span class="o">}</span>

<span class="c1">// Thread 2: same logic — both loop forever responding to each other</span>
<span class="k">while</span> <span class="o">(</span><span class="n">thread1IsRetrying</span><span class="o">)</span> <span class="o">{</span>
    <span class="nc">Thread</span><span class="o">.</span><span class="na">sleep</span><span class="o">(</span><span class="mi">100</span><span class="o">);</span>
    <span class="n">retry</span><span class="o">();</span>
<span class="o">}</span>
</code></pre></div></div>

<p><strong>Fix</strong> — introduce randomised backoff so threads don’t respond in perfect lockstep:</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">Thread</span><span class="o">.</span><span class="na">sleep</span><span class="o">(</span><span class="n">random</span><span class="o">.</span><span class="na">nextInt</span><span class="o">(</span><span class="mi">100</span><span class="o">));</span> <span class="c1">// random delay breaks the symmetry</span>
</code></pre></div></div>

<hr />

<h3 id="6-starvation">6. Starvation</h3>

<p>A thread is perpetually denied CPU time or lock access because other threads keep getting priority. The thread is alive but never makes progress.</p>

<p>Common causes:</p>
<ul>
  <li>Unfair locks (non-fair <code class="language-plaintext highlighter-rouge">ReentrantLock</code>)</li>
  <li>High-priority threads monopolising CPU</li>
  <li>A thread holding a lock for too long</li>
</ul>

<p><strong>Fix — use fair locks:</strong></p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// true = fair mode — threads acquire lock in arrival order</span>
<span class="nc">ReentrantLock</span> <span class="n">fairLock</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">ReentrantLock</span><span class="o">(</span><span class="kc">true</span><span class="o">);</span>
</code></pre></div></div>

<hr />

<h2 id="summary">Summary</h2>

<table>
  <thead>
    <tr>
      <th>Race Condition</th>
      <th>Root Cause</th>
      <th>Fix</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>Read-Modify-Write</strong></td>
      <td>Value changed between read and write-back</td>
      <td><code class="language-plaintext highlighter-rouge">AtomicInteger</code>, <code class="language-plaintext highlighter-rouge">synchronized</code></td>
    </tr>
    <tr>
      <td><strong>Check-Then-Act</strong></td>
      <td>Condition invalidated between check and act</td>
      <td><code class="language-plaintext highlighter-rouge">putIfAbsent</code>, <code class="language-plaintext highlighter-rouge">computeIfAbsent</code>, <code class="language-plaintext highlighter-rouge">synchronized</code></td>
    </tr>
    <tr>
      <td><strong>Stale Data</strong></td>
      <td>CPU cache / compiler reordering hides writes</td>
      <td><code class="language-plaintext highlighter-rouge">volatile</code>, <code class="language-plaintext highlighter-rouge">synchronized</code></td>
    </tr>
    <tr>
      <td><strong>Deadlock</strong></td>
      <td>Circular lock dependency</td>
      <td>Consistent lock ordering, <code class="language-plaintext highlighter-rouge">tryLock</code></td>
    </tr>
    <tr>
      <td><strong>Livelock</strong></td>
      <td>Circular active response with no progress</td>
      <td>Randomised backoff</td>
    </tr>
    <tr>
      <td><strong>Starvation</strong></td>
      <td>Unfair resource scheduling</td>
      <td>Fair locks, avoid long critical sections</td>
    </tr>
  </tbody>
</table>

<hr />

<h2 id="the-underlying-principle">The Underlying Principle</h2>

<p>Every race condition is a variation of the same mistake: <strong>assuming the world stays still between two operations</strong>. The window of time between any two statements is enough for another thread to change everything.</p>

<p>The solution is always to either:</p>
<ol>
  <li><strong>Make the compound action atomic</strong> — so no thread can observe intermediate state</li>
  <li><strong>Eliminate shared mutable state</strong> — immutable objects and local variables can’t race</li>
  <li><strong>Coordinate access</strong> — locks, <code class="language-plaintext highlighter-rouge">volatile</code>, or higher-level concurrent utilities</li>
</ol>

<p>The <code class="language-plaintext highlighter-rouge">java.util.concurrent</code> package exists precisely to give you safe, well-tested abstractions so you don’t have to manage this yourself. Prefer <code class="language-plaintext highlighter-rouge">ConcurrentHashMap</code> over <code class="language-plaintext highlighter-rouge">HashMap + synchronized</code>. Prefer <code class="language-plaintext highlighter-rouge">AtomicInteger</code> over <code class="language-plaintext highlighter-rouge">int + synchronized</code>. Prefer <code class="language-plaintext highlighter-rouge">ExecutorService</code> over raw <code class="language-plaintext highlighter-rouge">Thread</code>.</p>

<hr />

<h2 id="further-reading">Further Reading</h2>

<ul>
  <li><strong>Java Concurrency in Practice</strong> — Brian Goetz et al. <em>(the definitive reference)</em></li>
  <li><strong>The Art of Multiprocessor Programming</strong> — Herlihy &amp; Shavit <em>(for lock-free deep dives)</em></li>
  <li><strong>A Critique of ANSI SQL Isolation Levels</strong> — Berenson et al. <em>(same concepts, database context)</em></li>
  <li><strong>JSR-133 Java Memory Model</strong> — the formal spec behind <code class="language-plaintext highlighter-rouge">volatile</code> and <code class="language-plaintext highlighter-rouge">synchronized</code></li>
</ul>

<hr />

<p><em>Understanding race conditions is not about memorising patterns — it’s about building a mental model where you always ask: “what happens if another thread runs here?” Once that instinct is second nature, your concurrent code becomes dramatically safer.</em></p>]]></content><author><name></name></author><category term="technical" /><summary type="html"><![CDATA[Note: This blogs are generated by Claude.]]></summary></entry><entry><title type="html">99. The Evolution of Trust: From HTTP to CA Chains</title><link href="/technical/99-the-evolution-of-trust-from-http-to-ca-chains" rel="alternate" type="text/html" title="99. The Evolution of Trust: From HTTP to CA Chains" /><published>2026-03-09T08:02:00+00:00</published><updated>2026-03-09T08:02:00+00:00</updated><id>/technical/99-the-evolution-of-trust-from-http-to-ca-chains</id><content type="html" xml:base="/technical/99-the-evolution-of-trust-from-http-to-ca-chains"><![CDATA[<p>Note: This article is generated by Gemini.</p>

<ul>
  <li><a href="#the-evolution-of-trust-from-http-to-ca-chains">The Evolution of Trust: From HTTP to CA Chains</a>
    <ul>
      <li><a href="#1-the-vulnerable-start-plaintext-http">1. The Vulnerable Start: Plaintext HTTP</a></li>
      <li><a href="#2-the-first-fix-symmetric-encryption">2. The First Fix: Symmetric Encryption</a></li>
      <li><a href="#3-the-second-fix-asymmetric-async-encryption">3. The Second Fix: Asymmetric (Async) Encryption</a></li>
      <li><a href="#4-the-ultimate-fix-the-certificate-authority-ca">4. The Ultimate Fix: The Certificate Authority (CA)</a>
        <ul>
          <li><a href="#how-the-ca-works">How the CA works:</a></li>
        </ul>
      </li>
      <li><a href="#5-the-chain-of-trust-the-hierarchy">5. The Chain of Trust (The Hierarchy)</a></li>
      <li><a href="#6-overview-the-tls-handshake">6. Overview: The TLS Handshake</a></li>
      <li><a href="#7-the-verification-climb-how-the-client-walks-the-chain">7. The Verification Climb: How the Client “Walks” the Chain</a>
        <ul>
          <li><a href="#step-1-the-website-certificate-the-leaf">Step 1: The Website Certificate (The Leaf)</a></li>
          <li><a href="#step-2-the-intermediate-ca">Step 2: The Intermediate CA</a></li>
          <li><a href="#step-3-the-root-ca-the-anchor">Step 3: The Root CA (The Anchor)</a></li>
          <li><a href="#step-4-trust-established">Step 4: Trust Established</a></li>
          <li><a href="#why-this-middleman-intermediate-exists">Why this “Middleman” (Intermediate) exists</a></li>
          <li><a href="#summary-for-your-blog-the-trust-math">Summary for your blog: The “Trust Math”</a></li>
        </ul>
      </li>
    </ul>
  </li>
</ul>

<h2 id="the-evolution-of-trust-from-http-to-ca-chains">The Evolution of Trust: From HTTP to CA Chains</h2>

<h3 id="1-the-vulnerable-start-plaintext-http">1. The Vulnerable Start: Plaintext HTTP</h3>

<p>In the early days, HTTP was like sending a postcard through the mail. Anyone who handles the postcard—your ISP, a public Wi-Fi admin, or a hacker—can read exactly what is written on it.</p>

<ul>
  <li><strong>The Problem:</strong> There is no privacy.</li>
  <li><strong>The Risk:</strong> If you send a password, it’s visible in “plaintext.”</li>
</ul>

<h3 id="2-the-first-fix-symmetric-encryption">2. The First Fix: Symmetric Encryption</h3>

<p>To solve privacy, we could use <strong>Symmetric Encryption</strong>. Both the user and the website agree on a single “secret key” to scramble the data.</p>

<ul>
  <li><strong>The New Problem:</strong> How do you share that secret key in the first place? If you send the key over the internet to the server, a hacker can just intercept the key and use it to decrypt everything.</li>
</ul>

<h3 id="3-the-second-fix-asymmetric-async-encryption">3. The Second Fix: Asymmetric (Async) Encryption</h3>

<p>This introduces a pair of keys: a <strong>Public Key</strong> (which anyone can see) and a <strong>Private Key</strong> (which stays on the server).</p>

<ol>
  <li>Your browser gets the server’s <strong>Public Key</strong>.</li>
  <li>It uses that key to encrypt a message.</li>
  <li><strong>Only</strong> the server’s <strong>Private Key</strong> can decrypt that message.</li>
</ol>

<ul>
  <li><strong>The Critical Flaw:</strong> This solves privacy, but it doesn’t solve <strong>Identity</strong>.</li>
  <li><strong>The “Man-in-the-Middle” (MITM) Attack:</strong> A hacker could intercept your request and send you <em>their</em> public key instead of the bank’s. You would encrypt your data for the hacker, thinking they are the bank. You have a secure connection to the wrong person.</li>
</ul>

<h3 id="4-the-ultimate-fix-the-certificate-authority-ca">4. The Ultimate Fix: The Certificate Authority (CA)</h3>

<p>We need a way to verify that the Public Key actually belongs to the person we think it does. This is where the <strong>CA</strong> comes in.</p>

<p>The CA acts as a “Digital Notary.” Instead of the server just handing you a raw public key, it hands you a <strong>Digital Certificate</strong>. This certificate contains the public key and is “signed” by a CA.</p>

<h4 id="how-the-ca-works">How the CA works:</h4>

<ol>
  <li><strong>The Request:</strong> The website owner sends their public key to a CA (like DigiCert or Let’s Encrypt).</li>
  <li><strong>The Verification:</strong> The CA verifies the owner actually owns the domain.</li>
  <li><strong>The Signature:</strong> The CA uses its own <strong>Private Key</strong> to write a digital signature on the certificate.</li>
  <li><strong>The Trust:</strong> Your browser comes pre-installed with the CA’s <strong>Public Key</strong> (the Root). It uses that to verify the signature. If the math matches, you know the certificate is authentic.</li>
</ol>

<h3 id="5-the-chain-of-trust-the-hierarchy">5. The Chain of Trust (The Hierarchy)</h3>

<p>To keep the “Master” (Root) keys safe, CAs use a middleman.</p>

<ul>
  <li><strong>Root CA:</strong> Stored in a highly secure, offline vault. It signs the Intermediate.</li>
  <li><strong>Intermediate CA:</strong> Does the day-to-day work of signing website certificates.</li>
  <li><strong>End-Entity Certificate:</strong> The certificate on your specific website.</li>
</ul>

<p>When your browser checks a site, it follows the breadcrumbs:</p>

<blockquote>
  <p><strong>Website</strong> ==&gt; <strong>Intermediate</strong> ==&gt; <strong>Root</strong>.</p>
</blockquote>

<p>If the chain is unbroken and ends at a Root CA in your browser’s “Trust Store,” the connection is marked with that satisfying green lock icon.</p>

<h3 id="6-overview-the-tls-handshake">6. Overview: The TLS Handshake</h3>

<p>The TLS Handshake is simply the technical dance that puts all these pieces together:</p>

<ol>
  <li><strong>Hello:</strong> Agreement on encryption versions.</li>
  <li><strong>Certificate Exchange:</strong> The server proves its identity using the <strong>CA Chain</strong>.</li>
  <li><strong>Key Generation:</strong> Using <strong>Asymmetric Encryption</strong>, the two parties safely agree on a <strong>Symmetric Key</strong>.</li>
  <li><strong>Finished:</strong> They switch to Symmetric encryption (which is faster) for the rest of the session.</li>
</ol>

<h3 id="7-the-verification-climb-how-the-client-walks-the-chain">7. The Verification Climb: How the Client “Walks” the Chain</h3>

<p>Think of it as a <strong>Chain of Signatures</strong>. Each certificate is a document that says: <em>“I am [Name], here is my Public Key, and this claims is verified by [Issuer].”</em></p>

<p>When you connect to a server, it sends you a “Bundle.” This bundle usually contains the <strong>Website Certificate</strong> and the <strong>Intermediate CA Certificate</strong>. It does <em>not</em> usually send the Root CA certificate (because you should already have that).</p>

<p>Here is how the client (your browser) processes that bundle:</p>

<h4 id="step-1-the-website-certificate-the-leaf">Step 1: The Website Certificate (The Leaf)</h4>

<p>The browser looks at the certificate provided by <code class="language-plaintext highlighter-rouge">example.com</code>.</p>

<ul>
  <li><strong>The Signature:</strong> The browser sees this certificate was signed by “Intermediate CA.”</li>
  <li><strong>The Action:</strong> The browser goes to the next item in the bundle to find “Intermediate CA.”</li>
</ul>

<h4 id="step-2-the-intermediate-ca">Step 2: The Intermediate CA</h4>

<p>The browser examines the <strong>Intermediate CA Certificate</strong>.</p>

<ul>
  <li><strong>The Validation:</strong> The browser takes the <strong>Public Key</strong> found inside this Intermediate certificate and uses it to perform a math check on the Website Certificate’s signature.</li>
  <li><strong>The Result:</strong> If the math works, the browser now knows: <em>“Okay, the Website Certificate is definitely authentic… IF I can trust this Intermediate CA.”</em></li>
</ul>

<h4 id="step-3-the-root-ca-the-anchor">Step 3: The Root CA (The Anchor)</h4>

<p>Now the browser looks at who signed the Intermediate CA. It sees the issuer is “GlobalTrust Root CA.”</p>

<ul>
  <li><strong>The Search:</strong> The browser does <strong>not</strong> look in the bundle sent by the server. Instead, it looks in its own local <strong>Trust Store</strong> (a protected file in Windows, macOS, or Chrome).</li>
  <li><strong>The Match:</strong> It finds “GlobalTrust Root CA” pre-installed.</li>
  <li><strong>The Final Validation:</strong> It takes the <strong>Public Key</strong> from its own local Root certificate and uses it to verify the signature on the Intermediate CA certificate.</li>
</ul>

<h4 id="step-4-trust-established">Step 4: Trust Established</h4>

<p>If the Root CA’s public key successfully verifies the Intermediate, and the Intermediate’s public key successfully verifies the Website, the “Chain of Trust” is complete.</p>

<h4 id="why-this-middleman-intermediate-exists">Why this “Middleman” (Intermediate) exists</h4>

<p>You might wonder: <em>Why not just have the Root sign the website directly?</em></p>

<ol>
  <li><strong>Risk Mitigation:</strong> The Root CA’s private key is the “Master Key” to the internet. It is kept in a physical vault, often on a computer with no internet access.</li>
  <li><strong>Scalability:</strong> If a CA needs to revoke a compromised key, it’s much easier to revoke an Intermediate CA than to tell every computer on earth to delete a Root CA.</li>
  <li><strong>Isolation:</strong> If an Intermediate CA is hacked, the Root remains safe. The CA can just “kill” that branch of the tree and issue a new Intermediate.</li>
</ol>

<h4 id="summary-for-your-blog-the-trust-math">Summary for your blog: The “Trust Math”</h4>

<p>To put it in a simple formula for your readers:</p>

<ul>
  <li><strong>Root Public Key</strong> verifies ==&gt; <strong>Intermediate Certificate</strong></li>
  <li><strong>Intermediate Public Key</strong> verifies ==&gt; <strong>Website Certificate</strong></li>
  <li><strong>Website Public Key</strong> encrypts ==&gt; <strong>Your Data</strong></li>
</ul>

<blockquote>
  <p><strong>Note:</strong> If any link in this chain is missing, expired, or signed by someone not in your “Trust Store,” your browser will throw the “Your connection is not private” warning.</p>
</blockquote>

<hr />]]></content><author><name></name></author><category term="technical" /><summary type="html"><![CDATA[Note: This article is generated by Gemini.]]></summary></entry><entry><title type="html">98. Keytool vs openssl for Java</title><link href="/technical/98-keytool-vs-openssl-for-java" rel="alternate" type="text/html" title="98. Keytool vs openssl for Java" /><published>2026-03-06T08:02:00+00:00</published><updated>2026-03-06T08:02:00+00:00</updated><id>/technical/98-keytool-vs-openssl-for-java</id><content type="html" xml:base="/technical/98-keytool-vs-openssl-for-java"><![CDATA[<p>Note: This article is generated by Gemini.</p>

<p>This technical deep-dive explores the nuances of managing SSL Trust Anchors in Java 23, specifically focusing on Let’s Encrypt certificates and why standard OpenSSL exports often fail where Java’s native tools succeed.</p>

<hr />

<h1 id="understanding-java-trust-anchors-a-lets-encrypt-case-study">Understanding Java Trust Anchors: A Let’s Encrypt Case Study</h1>

<p>When developing or debugging secure Java applications, you will inevitably encounter the dreaded <code class="language-plaintext highlighter-rouge">javax.net.ssl.SSLHandshakeException</code>. This guide documents the process of isolating a specific Root Certificate Authority (CA) and forcing Java to use it as the sole “Trust Anchor.”</p>

<h2 id="1-locating-the-lets-encrypt-root-on-macos">1. Locating the Let’s Encrypt Root on macOS</h2>

<p>On a MacBook, the OS maintains its own trusted certificates. To test a connection manually, we first need to extract the <strong>ISRG Root X1</strong> (the primary root for Let’s Encrypt).</p>

<p><strong>The Command:</strong></p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>security find-certificate <span class="nt">-c</span> <span class="s2">"ISRG Root X1"</span> <span class="nt">-p</span> /System/Library/Keychains/SystemRootCertificates.keychain <span class="o">&gt;</span> isrg_root.pem

</code></pre></div></div>

<h2 id="2-the-trust-anchor-problem">2. The “Trust Anchor” Problem</h2>

<p>In Java, a “Trust Anchor” is a certificate you have explicitly decided to trust. If you tell Java to use a custom trust store, it ignores the thousands of default certificates (like Google, Amazon, or Digicert) and relies <strong>only</strong> on what you provide.</p>

<h3 id="the-common-error">The Common Error</h3>

<p>If you pass a raw <code class="language-plaintext highlighter-rouge">.pem</code> file to the Java <code class="language-plaintext highlighter-rouge">trustStore</code> property, you will likely see:</p>

<blockquote>
  <p><code class="language-plaintext highlighter-rouge">java.security.InvalidAlgorithmParameterException: the trustAnchors parameter must be non-empty</code></p>
</blockquote>

<p>This occurs because Java expects a <strong>KeyStore container</strong> (a structured database), not a flat text file. If it cannot parse the file, it finds zero certificates, and the “anchor” list remains empty.</p>

<hr />

<h2 id="3-jks-vs-pkcs12-in-java-23">3. JKS vs. PKCS12 in Java 23</h2>

<p>While <strong>JKS</strong> (Java KeyStore) is the legacy format, <strong>PKCS12</strong> is the modern industry standard. However, Java 23 is picky about how these are structured.</p>

<h3 id="why-openssl-exports-often-fail">Why OpenSSL Exports Often Fail</h3>

<p>When you use <code class="language-plaintext highlighter-rouge">openssl pkcs12 -export</code> to create a trust store, it often misses a critical metadata tag: the <strong>TrustedCertificateEntry</strong>. OpenSSL sees the file as a “bundle of data,” while Java is looking for a “list of trusted authorities.” Without this internal flagging, Java sees the certs but refuses to use them as anchors.</p>

<h3 id="the-solution-using-keytool">The Solution: Using <code class="language-plaintext highlighter-rouge">keytool</code></h3>

<p>The most reliable way to create a Java-compatible trust store is using Java’s own <code class="language-plaintext highlighter-rouge">keytool</code>. This ensures the certificate is correctly labeled as a trusted entry.</p>

<p><strong>List trusted cert of Java</strong></p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># This searches your Java truststore for the Let's Encrypt root</span>
keytool <span class="nt">-list</span> <span class="nt">-v</span> <span class="nt">-cacerts</span> <span class="nt">-storepass</span> changeit | <span class="nb">grep</span> <span class="s2">"ISRG Root X1"</span>
</code></pre></div></div>

<p><strong>Creating the Store:</strong></p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>keytool <span class="nt">-importcert</span> <span class="nt">-file</span> isrg_root.pem <span class="nt">-keystore</span> learning.p12 <span class="nt">-storetype</span> PKCS12 <span class="nt">-alias</span> letsencrypt <span class="nt">-storepass</span> changeit <span class="nt">-noprompt</span>

</code></pre></div></div>

<hr />

<h2 id="4-technical-implementation-java-23">4. Technical Implementation (Java 23)</h2>

<p>The following code can be used to verify if your custom trust store is working correctly.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="nn">javax.net.ssl.SSLSocket</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">javax.net.ssl.SSLSocketFactory</span><span class="o">;</span>

<span class="kd">public</span> <span class="kd">class</span> <span class="nc">VerifyCert</span> <span class="o">{</span>
    <span class="kd">public</span> <span class="kd">static</span> <span class="kt">void</span> <span class="nf">main</span><span class="o">(</span><span class="nc">String</span><span class="o">[]</span> <span class="n">args</span><span class="o">)</span> <span class="o">{</span>
        <span class="nc">String</span> <span class="n">host</span> <span class="o">=</span> <span class="s">"letsencrypt.org"</span><span class="o">;</span>
        <span class="k">try</span> <span class="o">{</span>
            <span class="nc">SSLSocketFactory</span> <span class="n">factory</span> <span class="o">=</span> <span class="o">(</span><span class="nc">SSLSocketFactory</span><span class="o">)</span> <span class="nc">SSLSocketFactory</span><span class="o">.</span><span class="na">getDefault</span><span class="o">();</span>
            <span class="nc">SSLSocket</span> <span class="n">socket</span> <span class="o">=</span> <span class="o">(</span><span class="nc">SSLSocket</span><span class="o">)</span> <span class="n">factory</span><span class="o">.</span><span class="na">createSocket</span><span class="o">(</span><span class="n">host</span><span class="o">,</span> <span class="mi">443</span><span class="o">);</span>
            <span class="n">socket</span><span class="o">.</span><span class="na">startHandshake</span><span class="o">();</span>
            <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="s">"Handshake Successful: Trusted by custom Anchor."</span><span class="o">);</span>
            <span class="n">socket</span><span class="o">.</span><span class="na">close</span><span class="o">();</span>
        <span class="o">}</span> <span class="k">catch</span> <span class="o">(</span><span class="nc">Exception</span> <span class="n">e</span><span class="o">)</span> <span class="o">{</span>
            <span class="nc">System</span><span class="o">.</span><span class="na">err</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="s">"Handshake Failed: "</span> <span class="o">+</span> <span class="n">e</span><span class="o">.</span><span class="na">getMessage</span><span class="o">());</span>
        <span class="o">}</span>
    <span class="o">}</span>
<span class="o">}</span>

</code></pre></div></div>

<h3 id="execution">Execution</h3>

<p>Run the application by pointing specifically to your new PKCS12 file:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># compile </span>
javac VerifyCert.java

<span class="c"># run</span>
java <span class="nt">-Djavax</span>.net.ssl.trustStore<span class="o">=</span>learning.p12 <span class="se">\</span>
     <span class="nt">-Djavax</span>.net.ssl.trustStoreType<span class="o">=</span>PKCS12 <span class="se">\</span>
     <span class="nt">-Djavax</span>.net.ssl.trustStorePassword<span class="o">=</span>changeit <span class="se">\</span>
     VerifyCert

</code></pre></div></div>

<h2 id="5-summary-findings">5. Summary Findings</h2>

<ul>
  <li><strong>Java doesn’t use the Mac Keychain:</strong> It uses its own <code class="language-plaintext highlighter-rouge">cacerts</code> file or a custom trust store provided at runtime.</li>
  <li><strong>Format Matters:</strong> Use <code class="language-plaintext highlighter-rouge">PKCS12</code> for modern compatibility, but build it with <code class="language-plaintext highlighter-rouge">keytool</code> to ensure the “Trusted” metadata is present.</li>
  <li><strong>The “Empty” Error:</strong> This is almost always a sign that Java couldn’t parse your trust store file or didn’t find any certificates flagged as trusted within it.</li>
</ul>

<hr />]]></content><author><name></name></author><category term="technical" /><summary type="html"><![CDATA[Note: This article is generated by Gemini.]]></summary></entry><entry><title type="html">97. [Draft][Design] - Error code</title><link href="/technical/97-design-error-code" rel="alternate" type="text/html" title="97. [Draft][Design] - Error code" /><published>2025-10-20T08:02:00+00:00</published><updated>2025-10-20T08:02:00+00:00</updated><id>/technical/97-design-error-code</id><content type="html" xml:base="/technical/97-design-error-code"><![CDATA[<p>In any systems, clear and informative error code is important on understanding the system behaviour, especially on operation and tracing issues.</p>

<p>In this article, with the help of <strong>Claude.ai</strong>, I’m trying to propose an Error Code design for mutile modules applicaiton. Though, it can be used in Web API application too.</p>

<h1 id="context">Context</h1>
<p>On Spring Applicaiton, we have different modules, which can be called Database or Rest API. And these modules can be shared the same tables in database  or the same Rest API.</p>

<p>Error code have to</p>
<ul>
  <li>Descriptive: Include what went wrong and ideally what to do about it</li>
  <li>Consistency/Extensibility: Same format across the entire system and leave room for extension. Ex: Http 4xx is client error, 5xx is server error.</li>
  <li>Categorized: support application with different moudles, and can be shared the same DB/API resource.</li>
  <li><strong>Start minimal, add more when needed</strong>: Don’t over-design or use too detail error code.</li>
</ul>

<h1 id="proposal">Proposal</h1>

<p>Error code format:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>MODULE-CATEGORY-SEQUENCE
</code></pre></div></div>

<ul>
  <li><strong>MODULE</strong>: Application module (AUTH, USER, ORDER, PAYMENT, etc.)</li>
  <li><strong>CATEGORY</strong>: Error type (VAL, BIZ, DB, API, SYS, etc.)</li>
  <li><strong>SEQUENCE</strong>: Specific error number (001, 002, etc.)</li>
</ul>

<p>Example: ORDER-BIZ-001 → Order module, Business logic, Error #001</p>

<h2 id="architecture-decision-shared-vs-module-specific-errors">Architecture Decision: Shared vs Module-Specific Errors</h2>

<p><strong>Shared Infrastructure Errors (DB, SYS, NET, API).</strong></p>

<p>These are NOT module-specific because:</p>
<ul>
  <li>Same infrastructure used across all modules</li>
  <li>Same handling regardless of module</li>
  <li>Avoids code duplication</li>
  <li>No manual translation needed</li>
</ul>

<p>Use these across ALL modules:</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ASSET-DB-001: Database operation failed
ASSET-DB-002: Resource not found

ORD-API-001: Order service failed
ORD-API-002: Order service, submit transaction api failed. (Specific when needed)

PAY-API-001: Payment service failed

APP-SYS-001: Internal server error (This is critical and unexpected error.)
</code></pre></div></div>

<p><strong>Module-Specific Errors (VAL, BIZ)</strong>
These ARE module-specific because:</p>

<ul>
  <li>Different validation rules per module</li>
  <li>Different business logic per module</li>
  <li>Need to know which module’s business rule failed</li>
</ul>

<p>Use module prefix:</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>AUTH-VAL-001, AUTH-BIZ-001
SHIP-VAL-001, SHIP-BIZ-001
</code></pre></div></div>

<h2 id="validation-errors-val">Validation Errors (VAL)</h2>

<h3 id="core-philosophy">Core Philosophy</h3>
<p>Validation = checking <strong>input format/structure</strong> before business logic</p>
<ul>
  <li>Technical checks (format, type, length)</li>
  <li>No business context needed (often, no need external connection like API or DB)</li>
  <li>Happens at the API/input layer</li>
</ul>

<table>
  <thead>
    <tr>
      <th>Code</th>
      <th>Description</th>
      <th>Example</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>VAL-001</td>
      <td>Required field missing</td>
      <td>Missing email field</td>
    </tr>
    <tr>
      <td>VAL-002</td>
      <td>Invalid format</td>
      <td>Email doesn’t contain @</td>
    </tr>
    <tr>
      <td>VAL-003</td>
      <td>Value out of range</td>
      <td>Age = -5 or 999</td>
    </tr>
    <tr>
      <td>VAL-004</td>
      <td>Type mismatch</td>
      <td>Expected number, got string</td>
    </tr>
    <tr>
      <td>VAL-005</td>
      <td>Too long</td>
      <td>String exceeds max length</td>
    </tr>
    <tr>
      <td>VAL-006</td>
      <td>Too short</td>
      <td>Password &lt; 8 characters</td>
    </tr>
  </tbody>
</table>

<p><strong>When NOT to use VAL:</strong></p>
<ul>
  <li>❌ “Email already exists” → Use <strong>BIZ-001</strong> (business rule)</li>
  <li>❌ “Insufficient funds” → Use <strong>BIZ-002</strong> (business rule)</li>
  <li>❌ “Cannot delete order” → Use <strong>BIZ-003</strong> (business rule)</li>
</ul>

<hr />

<h2 id="business-logic-errors-biz">Business Logic Errors (BIZ)</h2>

<h3 id="core-philosophy-1">Core Philosophy</h3>
<p>Business = <strong>domain rules and state</strong> checking</p>
<ul>
  <li>Requires database/context check</li>
  <li>Business domain knowledge needed</li>
  <li>Happens after validation passes</li>
</ul>

<table>
  <thead>
    <tr>
      <th>Code</th>
      <th>Description</th>
      <th>Example</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>BIZ-001</td>
      <td>Resource already exists</td>
      <td>Email already registered</td>
    </tr>
    <tr>
      <td>BIZ-002</td>
      <td>Insufficient resources</td>
      <td>Not enough balance/stock</td>
    </tr>
    <tr>
      <td>BIZ-003</td>
      <td>Invalid state transition</td>
      <td>Cannot cancel completed order</td>
    </tr>
    <tr>
      <td>BIZ-004</td>
      <td>Constraint violation</td>
      <td>Cannot delete user with active orders</td>
    </tr>
    <tr>
      <td>BIZ-005</td>
      <td>Business rule violation</td>
      <td>Generic business rule failed</td>
    </tr>
    <tr>
      <td>BIZ-006</td>
      <td>Limit exceeded</td>
      <td>Exceeded daily withdrawal limit</td>
    </tr>
    <tr>
      <td>BIZ-007</td>
      <td>Resource expired</td>
      <td>Coupon/offer expired</td>
    </tr>
    <tr>
      <td>BIZ-008</td>
      <td>Operation not allowed</td>
      <td>Action not permitted in current state</td>
    </tr>
  </tbody>
</table>

<hr />

<h2 id="database-db-and-api-errors">Database (DB) and API Errors</h2>

<h3 id="core-philosophy-2">Core Philosophy</h3>
<p>Database/API = <strong>infrastructure failures</strong> you can’t control</p>
<ul>
  <li>Not validation errors</li>
  <li>Not business errors</li>
  <li>Technical failures only</li>
</ul>

<table>
  <thead>
    <tr>
      <th>Code</th>
      <th>Description</th>
      <th>When It Happens</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>DB-001</td>
      <td>Database operation failed</td>
      <td>Generic DB error, connection lost</td>
    </tr>
    <tr>
      <td>DB-002</td>
      <td>Resource not found</td>
      <td>Query returned no results</td>
    </tr>
    <tr>
      <td>API-001</td>
      <td>API operation failed</td>
      <td>Generic API error, connection lost</td>
    </tr>
  </tbody>
</table>

<p><strong>That’s it! Just 2 codes.</strong></p>

<p><strong>Why so few?</strong></p>
<ul>
  <li>As a consumer, you can’t handle most DB/API errors differently</li>
  <li>Specific DB errors (timeout, deadlock) are infrastructure concerns</li>
  <li>Most DB errors = log and return 500</li>
  <li>“Not found” is the only one users care about</li>
  <li>If there is a frequently fail on a specific operation, we can add additional error code there, and start monitoring it.</li>
</ul>

<h1 id="sample-code-on-java">Sample code on Java</h1>

<p>Define the ErrorCode class</p>
<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// ========== Enums ==========</span>

<span class="kd">public</span> <span class="kd">enum</span> <span class="nc">Module</span> <span class="o">{</span>
    <span class="no">AUTH</span><span class="o">,</span>
    <span class="no">USER</span><span class="o">,</span>
    <span class="no">ORDER</span><span class="o">,</span>
    <span class="no">PAYMENT</span><span class="o">,</span>
    <span class="no">PRODUCT</span><span class="o">,</span>
    <span class="no">NOTIF</span><span class="o">,</span>
    <span class="no">SYSTEM</span>  <span class="c1">// For non-module specific errors</span>
<span class="o">}</span>

<span class="kd">public</span> <span class="kd">enum</span> <span class="nc">ErrorCategory</span> <span class="o">{</span>
    <span class="no">VAL</span><span class="o">,</span>    <span class="c1">// Validation</span>
    <span class="no">BIZ</span><span class="o">,</span>    <span class="c1">// Business Logic</span>
    <span class="no">DB</span><span class="o">,</span>     <span class="c1">// Database (shared)</span>
    <span class="no">API</span><span class="o">,</span>    <span class="c1">// External API (shared)</span>
    <span class="no">SYS</span>     <span class="c1">// System (shared)</span>
<span class="o">}</span>

<span class="c1">// ========== ErrorCode Class ==========</span>

<span class="kd">public</span> <span class="kd">class</span> <span class="nc">ErrorCode</span> <span class="o">{</span>
    <span class="kd">private</span> <span class="kd">final</span> <span class="nc">String</span> <span class="n">code</span><span class="o">;</span>
    <span class="kd">private</span> <span class="kd">final</span> <span class="nc">String</span> <span class="n">description</span><span class="o">;</span>
    <span class="kd">private</span> <span class="kd">final</span> <span class="nc">Module</span> <span class="n">module</span><span class="o">;</span>
    <span class="kd">private</span> <span class="kd">final</span> <span class="nc">ErrorCategory</span> <span class="n">category</span><span class="o">;</span>

    <span class="kd">public</span> <span class="nf">ErrorCode</span><span class="o">(</span><span class="nc">Module</span> <span class="n">module</span><span class="o">,</span> <span class="nc">ErrorCategory</span> <span class="n">category</span><span class="o">,</span> <span class="kt">int</span> <span class="n">sequence</span><span class="o">,</span> 
                     <span class="nc">String</span> <span class="n">description</span><span class="o">)</span> <span class="o">{</span>
        <span class="c1">// For shared infrastructure errors (DB, API, INFRA, SYS), use SYSTEM module</span>
        <span class="k">if</span> <span class="o">(</span><span class="n">isSharedCategory</span><span class="o">(</span><span class="n">category</span><span class="o">))</span> <span class="o">{</span>
            <span class="k">this</span><span class="o">.</span><span class="na">code</span> <span class="o">=</span> <span class="nc">String</span><span class="o">.</span><span class="na">format</span><span class="o">(</span><span class="s">"%s-%03d"</span><span class="o">,</span> <span class="n">category</span><span class="o">.</span><span class="na">name</span><span class="o">(),</span> <span class="n">sequence</span><span class="o">);</span>
            <span class="k">this</span><span class="o">.</span><span class="na">module</span> <span class="o">=</span> <span class="nc">Module</span><span class="o">.</span><span class="na">SYSTEM</span><span class="o">;</span>
        <span class="o">}</span> <span class="k">else</span> <span class="o">{</span>
            <span class="c1">// For module-specific errors (VAL, BIZ)</span>
            <span class="k">this</span><span class="o">.</span><span class="na">code</span> <span class="o">=</span> <span class="nc">String</span><span class="o">.</span><span class="na">format</span><span class="o">(</span><span class="s">"%s-%s-%03d"</span><span class="o">,</span> <span class="n">module</span><span class="o">.</span><span class="na">name</span><span class="o">(),</span> <span class="n">category</span><span class="o">.</span><span class="na">name</span><span class="o">(),</span> <span class="n">sequence</span><span class="o">);</span>
            <span class="k">this</span><span class="o">.</span><span class="na">module</span> <span class="o">=</span> <span class="n">module</span><span class="o">;</span>
        <span class="o">}</span>
        
        <span class="k">this</span><span class="o">.</span><span class="na">category</span> <span class="o">=</span> <span class="n">category</span><span class="o">;</span>
        <span class="k">this</span><span class="o">.</span><span class="na">description</span> <span class="o">=</span> <span class="n">description</span><span class="o">;</span>
    <span class="o">}</span>
    
    <span class="kd">private</span> <span class="kt">boolean</span> <span class="nf">isSharedCategory</span><span class="o">(</span><span class="nc">ErrorCategory</span> <span class="n">category</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">return</span> <span class="n">category</span> <span class="o">==</span> <span class="nc">ErrorCategory</span><span class="o">.</span><span class="na">DB</span> <span class="o">||</span> 
               <span class="n">category</span> <span class="o">==</span> <span class="nc">ErrorCategory</span><span class="o">.</span><span class="na">API</span> <span class="o">||</span> 
               <span class="n">category</span> <span class="o">==</span> <span class="nc">ErrorCategory</span><span class="o">.</span><span class="na">SYS</span><span class="o">;</span>
    <span class="o">}</span>

    <span class="c1">// Getters</span>
    <span class="kd">public</span> <span class="nc">String</span> <span class="nf">getCode</span><span class="o">()</span> <span class="o">{</span> <span class="k">return</span> <span class="n">code</span><span class="o">;</span> <span class="o">}</span>
    <span class="kd">public</span> <span class="nc">String</span> <span class="nf">getDescription</span><span class="o">()</span> <span class="o">{</span> <span class="k">return</span> <span class="n">description</span><span class="o">;</span> <span class="o">}</span>
    <span class="kd">public</span> <span class="nc">Module</span> <span class="nf">getModule</span><span class="o">()</span> <span class="o">{</span> <span class="k">return</span> <span class="n">module</span><span class="o">;</span> <span class="o">}</span>
    <span class="kd">public</span> <span class="nc">ErrorCategory</span> <span class="nf">getCategory</span><span class="o">()</span> <span class="o">{</span> <span class="k">return</span> <span class="n">category</span><span class="o">;</span> <span class="o">}</span>

    <span class="nd">@Override</span>
    <span class="kd">public</span> <span class="nc">String</span> <span class="nf">toString</span><span class="o">()</span> <span class="o">{</span>
        <span class="k">return</span> <span class="n">code</span> <span class="o">+</span> <span class="s">": "</span> <span class="o">+</span> <span class="n">description</span><span class="o">;</span>
    <span class="o">}</span>
<span class="o">}</span> 
</code></pre></div></div>

<p>Assumse that each module has a fews ErrorCode, then, we can use 1 single class. if the business logic increase, we can separate error code of each module into separate class</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kd">class</span> <span class="nc">ErrorCodeConstants</span> <span class="o">{</span>
    
    <span class="c1">// ==================== SHARED INFRASTRUCTURE ERRORS ====================</span>
    <span class="c1">// These are used across ALL modules</span>
    
    <span class="c1">// Database Errors (DB-xxx)</span>
    <span class="kd">public</span> <span class="kd">static</span> <span class="kd">final</span> <span class="nc">ErrorCode</span> <span class="no">DB_OPERATION_FAILED</span> <span class="o">=</span> 
        <span class="k">new</span> <span class="nf">ErrorCode</span><span class="o">(</span><span class="nc">Module</span><span class="o">.</span><span class="na">SYSTEM</span><span class="o">,</span> <span class="nc">ErrorCategory</span><span class="o">.</span><span class="na">DB</span><span class="o">,</span> <span class="mi">1</span><span class="o">,</span> 
            <span class="s">"Database operation failed"</span><span class="o">);</span>
    
    <span class="kd">public</span> <span class="kd">static</span> <span class="kd">final</span> <span class="nc">ErrorCode</span> <span class="no">DB_RESOURCE_NOT_FOUND</span> <span class="o">=</span> 
        <span class="k">new</span> <span class="nf">ErrorCode</span><span class="o">(</span><span class="nc">Module</span><span class="o">.</span><span class="na">SYSTEM</span><span class="o">,</span> <span class="nc">ErrorCategory</span><span class="o">.</span><span class="na">DB</span><span class="o">,</span> <span class="mi">2</span><span class="o">,</span> 
            <span class="s">"Resource not found"</span><span class="o">);</span>
    
    <span class="c1">// API Errors (API-xxx)</span>
    <span class="kd">public</span> <span class="kd">static</span> <span class="kd">final</span> <span class="nc">ErrorCode</span> <span class="no">API_EXTERNAL_SERVICE_FAILED</span> <span class="o">=</span> 
        <span class="k">new</span> <span class="nf">ErrorCode</span><span class="o">(</span><span class="nc">Module</span><span class="o">.</span><span class="na">SYSTEM</span><span class="o">,</span> <span class="nc">ErrorCategory</span><span class="o">.</span><span class="na">API</span><span class="o">,</span> <span class="mi">1</span><span class="o">,</span> 
            <span class="s">"External service failed"</span><span class="o">);</span>
    
    <span class="kd">public</span> <span class="kd">static</span> <span class="kd">final</span> <span class="nc">ErrorCode</span> <span class="no">API_EXTERNAL_SERVICE_TIMEOUT</span> <span class="o">=</span> 
        <span class="k">new</span> <span class="nf">ErrorCode</span><span class="o">(</span><span class="nc">Module</span><span class="o">.</span><span class="na">SYSTEM</span><span class="o">,</span> <span class="nc">ErrorCategory</span><span class="o">.</span><span class="na">API</span><span class="o">,</span> <span class="mi">2</span><span class="o">,</span> 
            <span class="s">"External service timeout"</span><span class="o">);</span>
    
    <span class="kd">public</span> <span class="kd">static</span> <span class="kd">final</span> <span class="nc">ErrorCode</span> <span class="no">API_STRIPE_FAILED</span> <span class="o">=</span> 
        <span class="k">new</span> <span class="nf">ErrorCode</span><span class="o">(</span><span class="nc">Module</span><span class="o">.</span><span class="na">SYSTEM</span><span class="o">,</span> <span class="nc">ErrorCategory</span><span class="o">.</span><span class="na">API</span><span class="o">,</span> <span class="mi">10</span><span class="o">,</span> 
            <span class="s">"Stripe payment service failed"</span><span class="o">);</span>
    
    <span class="kd">public</span> <span class="kd">static</span> <span class="kd">final</span> <span class="nc">ErrorCode</span> <span class="no">API_EMAIL_SERVICE_FAILED</span> <span class="o">=</span> 
        <span class="k">new</span> <span class="nf">ErrorCode</span><span class="o">(</span><span class="nc">Module</span><span class="o">.</span><span class="na">SYSTEM</span><span class="o">,</span> <span class="nc">ErrorCategory</span><span class="o">.</span><span class="na">API</span><span class="o">,</span> <span class="mi">20</span><span class="o">,</span> 
            <span class="s">"Email service failed"</span><span class="o">);</span>
    
    
    <span class="c1">// System Errors (SYS-xxx)</span>
    <span class="kd">public</span> <span class="kd">static</span> <span class="kd">final</span> <span class="nc">ErrorCode</span> <span class="no">SYS_INTERNAL_ERROR</span> <span class="o">=</span> 
        <span class="k">new</span> <span class="nf">ErrorCode</span><span class="o">(</span><span class="nc">Module</span><span class="o">.</span><span class="na">SYSTEM</span><span class="o">,</span> <span class="nc">ErrorCategory</span><span class="o">.</span><span class="na">SYS</span><span class="o">,</span> <span class="mi">1</span><span class="o">,</span> 
            <span class="s">"Internal server error"</span><span class="o">);</span>
    
    <span class="c1">// ==================== AUTH MODULE ====================</span>
    
    <span class="c1">// Validation Errors</span>
    <span class="kd">public</span> <span class="kd">static</span> <span class="kd">final</span> <span class="nc">ErrorCode</span> <span class="no">AUTH_MISSING_CREDENTIALS</span> <span class="o">=</span> 
        <span class="k">new</span> <span class="nf">ErrorCode</span><span class="o">(</span><span class="nc">Module</span><span class="o">.</span><span class="na">AUTH</span><span class="o">,</span> <span class="nc">ErrorCategory</span><span class="o">.</span><span class="na">VAL</span><span class="o">,</span> <span class="mi">1</span><span class="o">,</span> 
            <span class="s">"Missing credentials"</span><span class="o">);</span>
    
    <span class="c1">// Business Logic Errors</span>
    <span class="kd">public</span> <span class="kd">static</span> <span class="kd">final</span> <span class="nc">ErrorCode</span> <span class="no">AUTH_INVALID_CREDENTIALS</span> <span class="o">=</span> 
        <span class="k">new</span> <span class="nf">ErrorCode</span><span class="o">(</span><span class="nc">Module</span><span class="o">.</span><span class="na">AUTH</span><span class="o">,</span> <span class="nc">ErrorCategory</span><span class="o">.</span><span class="na">BIZ</span><span class="o">,</span> <span class="mi">1</span><span class="o">,</span> 
            <span class="s">"Invalid credentials"</span><span class="o">);</span>
    
    <span class="kd">public</span> <span class="kd">static</span> <span class="kd">final</span> <span class="nc">ErrorCode</span> <span class="no">AUTH_ACCOUNT_LOCKED</span> <span class="o">=</span> 
        <span class="k">new</span> <span class="nf">ErrorCode</span><span class="o">(</span><span class="nc">Module</span><span class="o">.</span><span class="na">AUTH</span><span class="o">,</span> <span class="nc">ErrorCategory</span><span class="o">.</span><span class="na">BIZ</span><span class="o">,</span> <span class="mi">2</span><span class="o">,</span> 
            <span class="s">"Account locked"</span><span class="o">);</span>
<span class="o">}</span>

</code></pre></div></div>

<p>ApplicationException class. Beside errorCode, we need to add the context object to provide additional information on how the exception is trigger.</p>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="kd">class</span> <span class="nc">ApplicationException</span> <span class="kd">extends</span> <span class="nc">RuntimeException</span> <span class="o">{</span>
    <span class="kd">private</span> <span class="kd">final</span> <span class="nc">ErrorCode</span> <span class="n">errorCode</span><span class="o">;</span>
    <span class="kd">private</span> <span class="kd">final</span> <span class="nc">Map</span><span class="o">&lt;</span><span class="nc">String</span><span class="o">,</span> <span class="nc">Object</span><span class="o">&gt;</span> <span class="n">context</span><span class="o">;</span>
    
    <span class="kd">public</span> <span class="nf">ApplicationException</span><span class="o">(</span><span class="nc">ErrorCode</span> <span class="n">errorCode</span><span class="o">,</span> <span class="nc">String</span> <span class="n">message</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">this</span><span class="o">(</span><span class="n">errorCode</span><span class="o">,</span> <span class="n">message</span><span class="o">,</span> <span class="kc">null</span><span class="o">,</span> <span class="k">new</span> <span class="nc">HashMap</span><span class="o">&lt;&gt;());</span>
    <span class="o">}</span>
    
    <span class="kd">public</span> <span class="nf">ApplicationException</span><span class="o">(</span><span class="nc">ErrorCode</span> <span class="n">errorCode</span><span class="o">,</span> <span class="nc">String</span> <span class="n">message</span><span class="o">,</span> <span class="nc">Throwable</span> <span class="n">cause</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">this</span><span class="o">(</span><span class="n">errorCode</span><span class="o">,</span> <span class="n">message</span><span class="o">,</span> <span class="n">cause</span><span class="o">,</span> <span class="k">new</span> <span class="nc">HashMap</span><span class="o">&lt;&gt;());</span>
    <span class="o">}</span>
    
    <span class="kd">public</span> <span class="nf">ApplicationException</span><span class="o">(</span><span class="nc">ErrorCode</span> <span class="n">errorCode</span><span class="o">,</span> <span class="nc">String</span> <span class="n">message</span><span class="o">,</span> 
                               <span class="nc">Throwable</span> <span class="n">cause</span><span class="o">,</span> <span class="nc">Map</span><span class="o">&lt;</span><span class="nc">String</span><span class="o">,</span> <span class="nc">Object</span><span class="o">&gt;</span> <span class="n">context</span><span class="o">)</span> <span class="o">{</span>
        <span class="kd">super</span><span class="o">(</span><span class="nc">String</span><span class="o">.</span><span class="na">format</span><span class="o">(</span><span class="s">"[%s] %s - %s"</span><span class="o">,</span> 
              <span class="n">errorCode</span><span class="o">.</span><span class="na">getCode</span><span class="o">(),</span> <span class="n">errorCode</span><span class="o">.</span><span class="na">getDescription</span><span class="o">(),</span> <span class="n">message</span><span class="o">),</span> <span class="n">cause</span><span class="o">);</span>
        <span class="k">this</span><span class="o">.</span><span class="na">errorCode</span> <span class="o">=</span> <span class="n">errorCode</span><span class="o">;</span>
        <span class="k">this</span><span class="o">.</span><span class="na">context</span> <span class="o">=</span> <span class="n">context</span><span class="o">;</span>
    <span class="o">}</span>
    
    <span class="kd">public</span> <span class="nc">ErrorCode</span> <span class="nf">getErrorCode</span><span class="o">()</span> <span class="o">{</span> <span class="k">return</span> <span class="n">errorCode</span><span class="o">;</span> <span class="o">}</span>
    <span class="kd">public</span> <span class="nc">Map</span><span class="o">&lt;</span><span class="nc">String</span><span class="o">,</span> <span class="nc">Object</span><span class="o">&gt;</span> <span class="nf">getContext</span><span class="o">()</span> <span class="o">{</span> <span class="k">return</span> <span class="n">context</span><span class="o">;</span> <span class="o">}</span>
    
    <span class="kd">public</span> <span class="nc">ApplicationException</span> <span class="nf">addContext</span><span class="o">(</span><span class="nc">String</span> <span class="n">key</span><span class="o">,</span> <span class="nc">Object</span> <span class="n">value</span><span class="o">)</span> <span class="o">{</span>
        <span class="k">this</span><span class="o">.</span><span class="na">context</span><span class="o">.</span><span class="na">put</span><span class="o">(</span><span class="n">key</span><span class="o">,</span> <span class="n">value</span><span class="o">);</span>
        <span class="k">return</span> <span class="k">this</span><span class="o">;</span>
    <span class="o">}</span>


<span class="o">}</span>
</code></pre></div></div>]]></content><author><name></name></author><category term="technical" /><summary type="html"><![CDATA[In any systems, clear and informative error code is important on understanding the system behaviour, especially on operation and tracing issues.]]></summary></entry><entry><title type="html">96. [POC] - Key-value cache using Postgres</title><link href="/technical/96-poc-key-value-cache-with-postgres" rel="alternate" type="text/html" title="96. [POC] - Key-value cache using Postgres" /><published>2025-08-09T08:02:00+00:00</published><updated>2025-08-09T08:02:00+00:00</updated><id>/technical/96-poc-key-value-cache-with-postgres</id><content type="html" xml:base="/technical/96-poc-key-value-cache-with-postgres"><![CDATA[<p>Sometines, we can’t use Redis or Memcached for caching pupose. One reason is license problem and that technology hasn’t approved to be used inside your company. Instead, we need to design a solution that use existing infrastructure like PostgreSQL database.</p>

<p>Note: Using ChatGPT to plan the idea</p>

<h2 id="core-features">Core features</h2>

<p>We need to design a caching solution that satisfy these constrains:</p>

<ul>
  <li>Using Postgres</li>
  <li>Support TimeToLive or Expired time</li>
  <li>Support Least Recent Used (LRU) behaviour. We can active refresh cache that frequently used and/optional delete old cached</li>
  <li>Support unique refresh for key if the application running on multiple instances.</li>
  <li>Using background refresh job to simplify the app logic</li>
</ul>

<h2 id="design-and-important-sql-queries">Design and important SQL queries</h2>

<h3 id="table">Table</h3>

<div class="language-sql highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">CREATE</span> <span class="k">TABLE</span> <span class="n">cache_entries</span> <span class="p">(</span>
    <span class="k">key</span> <span class="nb">TEXT</span> <span class="k">PRIMARY</span> <span class="k">KEY</span><span class="p">,</span>
    <span class="n">value</span> <span class="n">JSONB</span><span class="p">,</span>
    <span class="n">expires_at</span> <span class="nb">TIMESTAMP</span><span class="p">,</span>       <span class="c1">-- When value is considered expired</span>
    <span class="n">last_accessed_at</span> <span class="nb">TIMESTAMP</span><span class="p">,</span> <span class="c1">-- timestamp every time the key is read from cache. Support LRU</span>
    <span class="n">refresh_started_at</span> <span class="nb">TIMESTAMP</span><span class="p">,</span>     <span class="c1">-- When value was last recomputed</span>
    <span class="n">is_refreshing</span> <span class="nb">BOOLEAN</span><span class="p">,</span>       <span class="c1">-- Used to prevent duplicate refresh</span>
    <span class="n">updated_at</span> <span class="nb">TIMESTAMP</span>
<span class="p">);</span>

</code></pre></div></div>

<h3 id="logics">Logics</h3>

<p><strong>User Request (can have multiple applicaitons) →</strong></p>
<ul>
  <li>Check cache_entries →
    <ul>
      <li>if fresh → use</li>
      <li>if expired → use (optional), refresh (optional)</li>
      <li>if missing → compute, insert to cache</li>
      <li>when a key is used, always record the last_accessed_at. Using batch update, remove duplicated key to reduce database stress.</li>
    </ul>
  </li>
</ul>

<p><strong>Batch Worker (can have multiple workers) →</strong></p>
<ul>
  <li>every X min →
    <ul>
      <li>find near-expired keys →</li>
      <li>acquire lock (UPDATE with conditions) →</li>
      <li>compute →</li>
      <li>update value, release lock</li>
    </ul>
  </li>
</ul>

<p><strong>Cleanup Job →</strong></p>
<ul>
  <li>delete old unused keys</li>
</ul>

<h3 id="sql-queries-for-batch-refresh-key">SQL queries for batch refresh key</h3>

<p>a. Select key to refresh</p>

<div class="language-sql highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">SELECT</span> <span class="k">key</span> <span class="k">FROM</span> <span class="n">cache_entries</span>
<span class="k">WHERE</span> <span class="n">expires_at</span> <span class="o">&lt;=</span> <span class="n">now</span><span class="p">()</span>
  <span class="k">AND</span> <span class="n">last_accessed_at</span> <span class="o">&gt;=</span> <span class="n">now</span><span class="p">()</span> <span class="o">-</span> <span class="n">interval</span> <span class="s1">'7 days'</span> <span class="c1">-- used in last 7 days</span>
  <span class="k">AND</span> <span class="p">(</span><span class="n">is_refreshing</span> <span class="o">=</span> <span class="k">false</span> <span class="k">OR</span> <span class="n">refresh_started_at</span> <span class="o">&lt;</span> <span class="n">now</span><span class="p">()</span> <span class="o">-</span> <span class="n">interval</span> <span class="s1">'5 minutes'</span><span class="p">)</span> <span class="c1">-- is not refreshing by other service or stuck at least 5 mintues (in case app refresh during refresh or network failure)</span>
<span class="k">ORDER</span> <span class="k">BY</span> <span class="n">expires_at</span> <span class="k">ASC</span>
<span class="k">LIMIT</span> <span class="mi">50</span><span class="p">;</span>
</code></pre></div></div>

<ul>
  <li>This ensures you don’t waste resources refreshing stale/inactive data.</li>
  <li>If refresh a key fail, make sure that key can be refresh again. For safety reason.</li>
</ul>

<p>b. Acquiring refresh lock</p>

<div class="language-sql highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">UPDATE</span> <span class="n">cache_entries</span>
<span class="k">SET</span> <span class="n">is_refreshing</span> <span class="o">=</span> <span class="k">true</span><span class="p">,</span>
    <span class="n">refresh_started_at</span> <span class="o">=</span> <span class="n">now</span><span class="p">()</span>
<span class="k">WHERE</span> <span class="k">key</span> <span class="o">=</span> <span class="o">?</span> <span class="k">AND</span> <span class="p">(</span><span class="n">is_refreshing</span> <span class="o">=</span> <span class="k">false</span> <span class="k">OR</span> <span class="n">refresh_started_at</span> <span class="o">&lt;</span> <span class="n">now</span><span class="p">()</span> <span class="o">-</span> <span class="n">interval</span> <span class="s1">'5 minutes'</span><span class="p">)</span>
</code></pre></div></div>

<ul>
  <li>Only one app instance should refresh the key. Use an atomic update query to acquire the lock</li>
  <li>The condition ensures you can reclaim the lock if it’s stuck older than 5 minutes (tunable)</li>
  <li>This returns 1 row affected only if the lock was acquired.</li>
</ul>

<p>c. Release lock after refresh</p>

<div class="language-sql highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">UPDATE</span> <span class="n">cache_entries</span>
<span class="k">SET</span> <span class="n">is_refreshing</span> <span class="o">=</span> <span class="k">false</span><span class="p">,</span>
    <span class="n">refresh_started_at</span> <span class="o">=</span> <span class="k">NULL</span><span class="p">,</span> <span class="c1">-- must be null</span>
    <span class="n">value</span> <span class="o">=</span> <span class="o">?</span><span class="p">,</span>                <span class="c1">-- optional: update with new value</span>
    <span class="n">expires_at</span> <span class="o">=</span> <span class="o">?</span>           <span class="c1">-- optional: update expiration time</span>

<span class="k">WHERE</span> <span class="k">key</span> <span class="o">=</span> <span class="o">?</span><span class="p">;</span>
</code></pre></div></div>

<p>d. LRU-style cleanup</p>

<div class="language-sql highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">DELETE</span> <span class="k">FROM</span> <span class="n">cache_entries</span>
<span class="k">WHERE</span> <span class="n">last_accessed_at</span> <span class="o">&lt;</span> <span class="n">now</span><span class="p">()</span> <span class="o">-</span> <span class="n">interval</span> <span class="s1">'7 days'</span>
</code></pre></div></div>

<p>e. Update last access</p>
<div class="language-sql highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">UPDATE</span> <span class="n">cache_entries</span>
<span class="k">SET</span> <span class="n">last_accessed_at</span> <span class="o">=</span> <span class="n">now</span><span class="p">()</span>
<span class="k">WHERE</span> <span class="k">key</span> <span class="o">=</span> <span class="o">?</span><span class="p">;</span>
</code></pre></div></div>

<h3 id="sample-code">Sample code</h3>
<p>https://github.com/NhatVu/proof-of-concept/tree/main/postgres-key-value-cache</p>]]></content><author><name></name></author><category term="technical" /><summary type="html"><![CDATA[Sometines, we can’t use Redis or Memcached for caching pupose. One reason is license problem and that technology hasn’t approved to be used inside your company. Instead, we need to design a solution that use existing infrastructure like PostgreSQL database.]]></summary></entry><entry><title type="html">95. A quick note about the performance of Java Mapping Framework (Baeldung)</title><link href="/technical/95-performance-of-java-mapping-frameworks" rel="alternate" type="text/html" title="95. A quick note about the performance of Java Mapping Framework (Baeldung)" /><published>2025-08-03T08:02:00+00:00</published><updated>2025-08-03T08:02:00+00:00</updated><id>/technical/95-performance-of-java-mapping-frameworks</id><content type="html" xml:base="/technical/95-performance-of-java-mapping-frameworks"><![CDATA[<p>According to [1], Creating large Java applications composed of multiple layers require using multiple models such as persistence model, domain model or so-called DTOs. Using multiple models for different application layers will require us to provide a way of mapping between beans.</p>

<p>Doing this manually can quickly create much boilerplate code and consume a lot of time. Luckily for us, there are multiple object mapping frameworks for Java.[1]</p>

<p>This article just take a quick note on the performance of Java Mapping frameworks, include MapStruct, ModelMapper, JMapper.</p>

<h3 id="benmarks">Benmarks</h3>

<p>The average running time per operation (in ms), the less is better:</p>

<table>
  <thead>
    <tr>
      <th>Framework Name</th>
      <th>Average Running Time (ms per operation)</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>MapStruct</td>
      <td>10⁻⁴</td>
    </tr>
    <tr>
      <td>JMapper</td>
      <td>10⁻⁴</td>
    </tr>
    <tr>
      <td>Orika</td>
      <td>0.007</td>
    </tr>
    <tr>
      <td>ModelMapper</td>
      <td>0.137</td>
    </tr>
    <tr>
      <td>Dozer</td>
      <td>0.145</td>
    </tr>
  </tbody>
</table>

<p>Throughput benmarks: the benchmark returns the number of operations per second (the more is better)</p>

<table>
  <thead>
    <tr>
      <th>Framework Name</th>
      <th>Throughput (operations per ms)</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>JMapper</td>
      <td>3205</td>
    </tr>
    <tr>
      <td>MapStruct</td>
      <td>3467</td>
    </tr>
    <tr>
      <td>Orika</td>
      <td>121</td>
    </tr>
    <tr>
      <td>ModelMapper</td>
      <td>7</td>
    </tr>
    <tr>
      <td>Dozer</td>
      <td>6.342</td>
    </tr>
  </tbody>
</table>

<p>Personally, I only had a chance to work with MapStruct and ModelMapper. Not working with other mapping framework.</p>

<h3 id="conclusion">Conclusion:</h3>

<ul>
  <li>MapStruct is far faster than ModelMapper</li>
  <li>MapStruct use compile-time code generation. The generation code is nearly the same way we write the convertion code manually.</li>
  <li>ModelMapper use reflection, less performance.</li>
  <li>MapStruct is more explicit when define mapping. ModelMapping provide more “intelligent” mapping option, but it can be hard to debug and understand. I’m not prefer it</li>
  <li>Both support nested object mapping</li>
</ul>

<h3 id="references">References</h3>

<ol>
  <li><a href="https://www.baeldung.com/java-performance-mapping-frameworks">Performance of Java Mapping frameworks</a></li>
</ol>]]></content><author><name></name></author><category term="technical" /><summary type="html"><![CDATA[According to [1], Creating large Java applications composed of multiple layers require using multiple models such as persistence model, domain model or so-called DTOs. Using multiple models for different application layers will require us to provide a way of mapping between beans.]]></summary></entry><entry><title type="html">94. Spring Framework: Resource</title><link href="/technical/94-spring-framework-resource" rel="alternate" type="text/html" title="94. Spring Framework: Resource" /><published>2025-04-05T08:02:00+00:00</published><updated>2025-04-05T08:02:00+00:00</updated><id>/technical/94-spring-framework-resource</id><content type="html" xml:base="/technical/94-spring-framework-resource"><![CDATA[<ul>
  <li><a href="#1-java-class-loader-1">1. Java Class loader [1]</a>
    <ul>
      <li><a href="#11-jar-hell">1.1. JAR hell</a></li>
      <li><a href="#12-classpath-env-variable">1.2. CLASSPATH env variable</a></li>
      <li><a href="#13-using-classloader-to-load-resource">1.3. Using ClassLoader to load resource</a></li>
    </ul>
  </li>
  <li><a href="#2-maven-project-structure">2. Maven project structure</a></li>
  <li><a href="#3-spring-framework-resource-2">3. Spring Framework: Resource [2]</a>
    <ul>
      <li><a href="#31-using-prefix">3.1. Using prefix</a></li>
    </ul>
  </li>
  <li><a href="#4-references">4. References</a></li>
</ul>

<p>This article talks about how to read resources (file) in Spring Boot applicaiton. Giving 2 ways to read resource and the different between them: Using ClassLoader (core Java) and using ResourceLoader (core Spring)</p>

<p>Note: using ChatGPT + copy some contents from reference source.</p>

<h3 id="1-java-class-loader-1">1. Java Class loader [1]</h3>
<p>The Java class loader, part of the Java Runtime Environment, dynamically loads Java classes into the Java Virtual Machine. Usually classes are only loaded on demand. The virtual machine will only load the class files required for executing the program.</p>

<p>This loading is typically done “on demand”, in that it does not occur until the class is called by the program. A class with a given name can only be loaded once by a given class loader.</p>

<p>When the JVM is started, three class loaders are used:</p>

<ul>
  <li>Bootstrap class loader: The bootstrap class loader loads the core Java libraries located in the <JAVA_HOME>/jre/lib (or <JAVA_HOME>/jmods&gt; for Java 9 and above) directory. This class loader, which is part of the core JVM, is written in native code. The bootstrap class loader is not associated with any ClassLoader object, for instance, StringBuilder.class.getClassLoader() returns null.</JAVA_HOME></JAVA_HOME></li>
  <li>Extensions class loader: loads the code in the extensions directories (<JAVA_HOME>/jre/lib/ext,[5] or any other directory specified by the java.ext.dirs system property).</JAVA_HOME></li>
  <li>System class loader: The system class loader loads code found on <strong>java.class.path</strong> env variable, which maps to the <strong>CLASSPATH</strong> environment variable.</li>
</ul>

<h4 id="11-jar-hell">1.1. JAR hell</h4>
<p>JAR hell used to describe all the various ways in which the classloading process can end up not working. JAR hell can occur are:</p>

<ul>
  <li>Accidental presence of two different versions of a library installed on a system. This will not be considered an error by the system. Rather, <strong>the system will load classes from one or the other library</strong>. Adding the new library to the list of available libraries instead of replacing it may result in the application still behaving as though the old library is in use, which it may well be.</li>
  <li>Multiple libraries or applications require different versions of library <strong>foo</strong>. If versions of library <strong>foo</strong> use the same class names, there is no way to load the versions of library <strong>foo</strong> with the same class loader.</li>
</ul>

<h4 id="12-classpath-env-variable">1.2. CLASSPATH env variable</h4>
<p>Classpath is a parameter in the Java Virtual Machine or the Java compiler that <strong>specifies the location of user-defined classes, packages, and resources (if using Maven project style)</strong>. The parameter may be set either on the command-line, or through an environment variable.</p>

<p>Setting the Classpath via the Command Line</p>
<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">--</span> Window based machine
java <span class="nt">-classpath</span> /path/to/class/files MyProgram

<span class="nt">--</span> Linux
java <span class="nt">-cp</span> /path/to/class/files MyProgram

</code></pre></div></div>

<h4 id="13-using-classloader-to-load-resource">1.3. Using ClassLoader to load resource</h4>
<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="nc">List</span><span class="o">&lt;</span><span class="nc">String</span><span class="o">&gt;</span> <span class="nf">readFileWithClassLoader</span><span class="o">(</span><span class="nc">String</span> <span class="n">filePath</span><span class="o">)</span> <span class="kd">throws</span> <span class="nc">IOException</span> <span class="o">{</span>
        <span class="nc">List</span><span class="o">&lt;</span><span class="nc">String</span><span class="o">&gt;</span> <span class="n">res</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">ArrayList</span><span class="o">&lt;&gt;();</span>
        <span class="c1">// return ClassLoader that use by resourceLoeader. It's jdk.internal.loader.ClassLoaders$AppClassLoader</span>
        <span class="nc">ClassLoader</span> <span class="n">classLoader</span> <span class="o">=</span> <span class="n">resourceLoader</span><span class="o">.</span><span class="na">getClassLoader</span><span class="o">();</span>
        <span class="k">if</span><span class="o">(</span><span class="n">classLoader</span> <span class="o">==</span> <span class="kc">null</span><span class="o">){</span>
            <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="s">"classloader is not exist"</span><span class="o">);</span>
            <span class="k">throw</span> <span class="k">new</span> <span class="nf">NullPointerException</span><span class="o">(</span><span class="s">"classloader is not exist"</span><span class="o">);</span>
        <span class="o">}</span>
        <span class="nc">System</span><span class="o">.</span><span class="na">out</span><span class="o">.</span><span class="na">println</span><span class="o">(</span><span class="s">"classLoader: "</span> <span class="o">+</span> <span class="n">classLoader</span><span class="o">.</span><span class="na">getClass</span><span class="o">().</span><span class="na">getName</span><span class="o">());</span>
        <span class="k">try</span> <span class="o">(</span><span class="nc">BufferedReader</span> <span class="n">bufferedReader</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">BufferedReader</span><span class="o">(</span><span class="k">new</span> <span class="nc">InputStreamReader</span><span class="o">(</span><span class="n">classLoader</span><span class="o">.</span><span class="na">getResourceAsStream</span><span class="o">(</span><span class="n">filePath</span><span class="o">)))){</span>
            <span class="nc">String</span> <span class="n">line</span><span class="o">;</span>
            <span class="k">while</span> <span class="o">((</span><span class="n">line</span> <span class="o">=</span> <span class="n">bufferedReader</span><span class="o">.</span><span class="na">readLine</span><span class="o">())</span> <span class="o">!=</span> <span class="kc">null</span><span class="o">)</span> <span class="o">{</span>
                <span class="n">res</span><span class="o">.</span><span class="na">add</span><span class="o">(</span><span class="n">line</span><span class="o">);</span>
            <span class="o">}</span>
        <span class="o">}</span>
        <span class="k">return</span> <span class="n">res</span><span class="o">;</span>
    <span class="o">}</span>
</code></pre></div></div>

<h3 id="2-maven-project-structure">2. Maven project structure</h3>

<p>Classpath Locations:</p>

<p><strong>Source Code:</strong></p>

<ul>
  <li>src/main/java: Contains the main application’s source code. All .class files generated during compilation are included in the classpath.</li>
  <li>src/test/java: Includes test classes, which are added to the classpath during testing.</li>
</ul>

<p><strong>Resources:</strong></p>
<ul>
  <li>src/main/resources: Non-code resources for the application (e.g., application.properties, XML files) are included in the classpath automatically.</li>
  <li>src/test/resources: Resources specifically for testing are added to the classpath when running tests.</li>
</ul>

<p><strong>Compiled Code:</strong></p>
<ul>
  <li>target/classes: After compiling your application, the .class files are stored here. Maven ensures this directory is part of the classpath.</li>
  <li>target/test-classes: After compiling test code, the generated .class files are stored here, included during test execution.</li>
</ul>

<p><strong>Dependencies:</strong>
Maven automatically downloads and manages dependencies specified in the pom.xml file. These JAR files are stored in the local repository (~/.m2/repository) and included in the classpath during builds.</p>

<p><img src="/assets/images/2025/95_maven_project_structure_output.png" alt="Maven_project_structure_output" /></p>

<p>As above image, our classpath is <strong>target/classes</strong>, and folder input inside <strong>src/main/resources</strong> folder is placed under <strong>target/classes</strong></p>

<h3 id="3-spring-framework-resource-2">3. Spring Framework: Resource [2]</h3>
<p>For more detail on Spring Framework Resource, read at [2].</p>

<p>The <em>ResourceLoader</em> interface is meant to be implemented by objects that can return (that is, load) <em>Resource</em> instances.</p>

<p>All application contexts implement the ResourceLoader interface. Therefore, <em>all application contexts may be used to obtain Resource instances</em>.</p>

<p>When you call getResource() on a specific application context, and the location path specified doesn’t have a specific prefix, you get back a Resource type that is appropriate to that particular application context. For example, <em>FileSystemXmlApplicationContext</em> instance, it would return a FileSystemResource. For a <em>WebApplicationContext</em>, it would return a <em>ServletContextResource</em> (this is the case for web development, using <em>spring-boot-starter-web</em>). It would similarly return appropriate objects for each context.</p>

<p>As a result, you can load resources in a fashion appropriate to the particular application context.</p>

<h4 id="31-using-prefix">3.1. Using prefix</h4>
<p>On the other hand, you may also force <em>ClassPathResource</em> to be used, regardless of the application context type, by specifying the special <strong>classpath:</strong> prefix. Similarly, you can force a UrlResource to be used by specifying any of the standard java.net.URL prefixes like file and https</p>

<p><img src="/assets/images/2025/95_resource_prefix.png" alt="Resource_prefix" /></p>

<p>When you don’t specify any prefix, Resource implementation class will depend on ApplicationContext.</p>

<p>Personally, I prefer this approach, as it gives us more information explicitly.</p>

<h3 id="4-references">4. References</h3>
<ol>
  <li><a href="https://en.wikipedia.org/wiki/Java_class_loader">Java Class Loader</a></li>
  <li><a href="https://docs.spring.io/spring-framework/reference/core/resources.html">Spring Framework: Resource</a></li>
</ol>]]></content><author><name></name></author><category term="technical" /><summary type="html"><![CDATA[1. Java Class loader [1] 1.1. JAR hell 1.2. CLASSPATH env variable 1.3. Using ClassLoader to load resource 2. Maven project structure 3. Spring Framework: Resource [2] 3.1. Using prefix 4. References]]></summary></entry></feed>